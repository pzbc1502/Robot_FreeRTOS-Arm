# Robot_FreeRTOS-Arm 机械臂控制文档

> 本文档记录当前工程的控制架构、已完成改动、待完成改动及实施计划。
> 参考文档：`../对比及改进方法.md`（Robot_FreeRTOS 与 MechanicalArm 对比分析）

---

## 1. 工程说明

本工程从 `Robot_FreeRTOS` 裁剪而来，**只保留单独机械臂控制**，已移除：
- 视觉采摘状态机（robot-take）
- 底盘控制（chassis）
- K230 视觉通信
- DHT11 温湿度、草莓计数等 MQTT 上报

保留内容：
- 6 轴机械臂运动控制（J1~J5 整臂，J6 夹爪独立）
- 串口指令控制（abs_rotate / rel_rotate / auto / hard_reset / soft_reset）
- 夹爪控制（gripper_stop / gripper_open / gripper_grasp）
- MQTT 远程控制（remote_enable / remote_event）
- W800 MQTT 关节状态同步

---

## 2. 当前控制架构

### 2.1 任务结构

```
robot_thread_entry()
    ├── BSP_UART_Init()
    ├── BSP_CAN_Init()
    └── robot_init()
            ├── 打开 6 路限位开关外部中断
            ├── 创建 event_queue（机械臂事件队列）
            ├── 创建 cmd_queue（串口/MQTT 指令队列）
            ├── 创建 robot_control_task（优先级 3）
            └── 创建 robot_cmd_service（优先级 2）

vision_service_thread_entry()   ← 已裁剪为空任务，仅保留线程入口
```

### 2.2 运动控制流程

```
串口 / MQTT 指令
        ↓
    cmd_queue
        ↓
robot_cmd_service → robot_uart1_handle / robot_mqtt_handle
        ↓
robot_send_xxx_event() → event_queue
        ↓
robot_control_task（主控循环）
        ↓
    按事件类型分发：
    ├── ROBOT_AUTO_EVENT        → robot_auto_move_interpolation()
    │       ├── 线性插值（1mm 间距）生成路径点
    │       ├── 逐点调用逆运动学求解关节角
    │       └── robot_pid_run() 执行轨迹
    ├── ROBOT_JOINT_ABS_ROTATE  → robot_joint_rotate_to()（位置模式）
    ├── ROBOT_JOINT_REL_ROTATE  → robot_joint_rotate_to()（位置模式）
    ├── ROBOT_SOFT_RESET_EVENT  → robot_joint_soft_reset()
    ├── ROBOT_HARD_RESET_EVENT  → robot_joint_hard_reset()
    └── ROBOT_REMOTE_CONTROL_EVENT → robot_pid_remote()
```

### 2.3 当前控制器（原始 PID）

```c
// 控制周期：20ms（50Hz）
// 每个路径点持续 100ms，内含 5 次 PID 循环
v = Kp * error + Ki * integral + Kd * derivative
// Kp=10.0  Ki=0.002  Kd=0.0
// 所有关节使用相同参数，无前馈，无输出限幅
```

### 2.4 关节参数

| 关节 | 默认角度 | 软限位 | 减速比 | 正方向 |
|------|----------|--------|--------|--------|
| J1 | 90° | 0°~360° | 50 | CCW |
| J2 | 90° | 90°~180° | 99.99 | CW |
| J3 | -90° | -90°~90° | 50.89 | CW |
| J4 | 0° | -90°~90° | 51 | CW |
| J5 | 90° | 0°~90° | 26.85 | CCW |
| J6 | 0° | 0°~360° | 51 | CW（夹爪独立控制）|

---

## 3. 已完成改动

> **注意：** 以下改动曾经实施，但因 `git revert` 操作被撤回，当前代码为原始状态，需要重新执行。

| 编号 | 改动内容 | 涉及文件 | 状态 |
|------|----------|----------|------|
| 裁剪 | 移除底盘、视觉采摘、K230通信、DHT11 | `src/robot_thread_entry.c`<br>`src/vision_service_thread_entry.c`<br>`Middle/robot_cmd.c`<br>`APP/robot.h` | ⚠️ 已撤回，需重做 |
| 9.8 | 修复 `robot_init` 中 6 处限位开关 `open()` 判断逻辑反转（`== FSP_SUCCESS` → `!= FSP_SUCCESS`） | `APP/robot.c` | ⚠️ 已撤回，需重做 |
| 9.6 | 将控制器从 PID 替换为前馈+P（来自 MechanicalArm），各关节独立 Kp，去掉积分项 | `APP/robot.c`<br>`APP/robot.h` | ⚠️ 已撤回，需重做 |

---

## 4. 待完成改动

按优先级排列，参考 `../对比及改进方法.md` 第 9 章。

### P0 — 立即执行（修复已知 Bug）

#### 4.1 工程裁剪（重做）
移除底盘、视觉采摘等无关模块，保持工程干净。

**涉及文件：**
- `src/robot_thread_entry.c`：去掉 `robot_chassis_app_init()`、K230、DHT11、ESP8266
- `src/vision_service_thread_entry.c`：替换为空任务
- `Middle/robot_cmd.c`：去掉底盘指令（ch_f/ch_b/ch_l/ch_r/ch_s）、take_enable/take_disable
- `APP/robot.h`：去掉 `ROBOT_TAKE_ENABLED` 声明

#### 4.2 修复初始化逻辑反转（9.8，重做）
`robot_init()` 中 6 处限位开关 `open()` 判断方向错误，成功时打印 failed。

**修改位置：** `APP/robot.c`，`robot_init()` 函数，约第 1169~1186 行

```c
// 错误（当前代码）
if (JOINT_LIMIT_1.p_api->open(...) == FSP_SUCCESS) {
    LOG("JOINT_LIMIT_1 open failed\r\n");  // 成功时反而报错
}

// 正确
if (JOINT_LIMIT_1.p_api->open(...) != FSP_SUCCESS) {
    LOG("JOINT_LIMIT_1 open failed\r\n");
}
```

#### 4.3 控制器替换为前馈+P（9.6，重做）
将原始 PID 替换为 MechanicalArm 工程的前馈+P 控制器，消除积分饱和风险。

**修改位置：** `APP/robot.c`，`robot_pid_one_period()` 函数

```c
// 各关节独立 Kp（来自 MechanicalArm，单位 1/s）
static const float ROBOT_JOINT_KP[6] = {0.65f, 0.50f, 0.75f, 1.00f, 1.00f, 10.0f};

// 前馈 + P
float feedforward = (target_angle[j] - last_target_angle[j]) / CONTROL_DT_S;
float p_out = ROBOT_JOINT_KP[j] * error;
float v = feedforward + p_out;
```

**同步修改 `APP/robot.h`：** 去掉 `ROBOT_PID_KI`、`ROBOT_PID_KD`，保留 `ROBOT_PID_PERIOD`。

---

### P1 — 短期（提升通信与控制频率）

#### 4.4 控制频率提升至 100Hz（9.3）

**目标：** PID 控制周期从 20ms（50Hz）降到 10ms（100Hz），让 PID 修正更密、跟踪误差更小。控制器仍沿用现有 PID（不改 Kp/Ki/Kd 公式）。

**现状（已具备前提）：**
- 4.5 CAN 并行化已完成，角度批量读取约 6~15ms。
- 但 `robot_pid_one_period()` 内部每周期还串行发 5 条**阻塞式**速度命令（见下），单周期实测 16~25ms，**直接改 10ms 无法达成**。

**核心障碍：速度命令是阻塞式请求-应答**

`robot_joint_veloccity_to()`（[APP/robot.c:156](APP/robot.c)）每次调用都 `BSP_CAN_Lock` → `Emm_V5_Vel_Control` → `BSP_CAN_WaitReply(0xF6, 50ms)` → `BSP_CAN_Unlock`。一个周期 5 个关节串行执行，等回包耗时叠加。

**参考来源：** MechanicalArm 的速度命令是 fire-and-forget——`MotorControl::Motor_VelControl`（`Cpp/Src/MotorControl.cpp:91`）只 `MyCAN1_Transmit(Data,8)` 就返回，**不等 0xF6 回包**。`Control_All_Motor`（`Cpp/Src/NewBee.cpp:134`）连续发 6 路速度命令即结束。

**时间预算（10ms 周期）：**
```
并行读 6 路角度      ~6ms  （需把 CAN 批量超时从 15ms 降到 6ms）
发 5 路速度命令(非阻塞) ~2~3ms（5 帧 × ~0.4ms/帧 TX）
PID 计算             <0.1ms
-------------------------------------
合计                 ~8~9ms  < 10ms  ✓
```

---

##### 步骤 1：新增非阻塞速度命令函数 `APP/robot.c`

不删原 `robot_joint_veloccity_to`（soft_reset/单关节仍用阻塞版），新增一个 PID 循环专用的非阻塞版本：

```c
/* PID 控制循环专用：发送速度命令但不等待回包（fire-and-forget）。
 * 参考 MechanicalArm Motor_VelControl，仅发送，靠下一周期读角度闭环校正。 */
static void robot_joint_velocity_nowait(uint32_t joint_id, float velocity, uint8_t acceleration)
{
    if (joint_id >= ROBOT_MAX_JOINT_NUM) {
        return;
    }
    struct joint *joint = &g_robot.joints[joint_id];
    uint8_t dir  = (velocity > 0.0f) ? (uint8_t)joint->postive_direction
                                     : (uint8_t)(!joint->postive_direction);
    uint8_t addr = (uint8_t)(joint_id + 1u);

    ROBOT_STATUS_CLEAR(joint->status, ROBOT_STATUS_LIMIT_ENABLE);
    joint->velocity = velocity;
    uint16_t rpm = (uint16_t)fabsf(velocity * 600.0f * joint->reduction_ratio / 360.0f);

    /* 与并行读角度共用总线，加锁但不等回包，发完立即释放 */
    if (!BSP_CAN_Lock(20u)) {
        return;
    }
    Emm_V5_Vel_Control(addr, dir, rpm, acceleration, false);
    BSP_CAN_Unlock();
}
```

> 注意：电机收到 0xF6 速度命令仍会回 `F6 02 6B`，这些回包会进入 ISR。由于 4.5 的 ISR 分发只在 `data[0]==0x36`（位置回包）时才写 per-motor 缓冲，`0xF6` 回包只会 give 一次 binary semaphore、被下次 `BSP_CAN_WaitReply` 的 `xSemaphoreTake` 清掉，不污染角度缓冲。无需额外处理。

##### 步骤 2：`robot_pid_one_period()` 改用非阻塞发送

```c
static void robot_pid_one_period(float *target_angle, float *intg_error,
                                 float *pre_error, float *total_error, int joint_num)
{
    float error = 0;
    float v;
    uint32_t pid_end_time = xTaskGetTickCount() + ROBOT_PID_PERIOD;

    (void)robot_update_all_angles((uint8_t)joint_num, NULL, NULL);

    for (int j = 0; j < joint_num; j++) {
        g_current_angle[j] = g_robot.joints[j].current_angle;
        error = robot_angle_diff(g_robot.joints[j].current_angle, target_angle[j]);
        intg_error[j] += error;
        if (total_error != NULL) {
            total_error[j] += fabsf(error);
        }
        v = ROBOT_PID_KP * error + ROBOT_PID_KI * intg_error[j] + ROBOT_PID_KD * (error - pre_error[j]);
        pre_error[j] = error;
        robot_joint_velocity_nowait((uint32_t)j, v, ROBOT_JOINT_DEFAULT_ACCELERATION); /* 改为非阻塞 */
    }

    uint32_t now = xTaskGetTickCount();
    if (now < pid_end_time) {
        vTaskDelay(pid_end_time - now);
    }
}
```

##### 步骤 3：CAN 批量超时降到 6ms `APP/robot.c`

`robot_update_all_angles()` 内（[APP/robot.c:521](APP/robot.c)）：

```c
bool all_ok = BSP_CAN_WaitAllMotors(joint_num, 6u);   /* 15u → 6u，避免超过 10ms 周期 */
```

##### 步骤 4：修改周期宏 `APP/robot.h`

```c
#define ROBOT_PID_PERIOD                    (10)    /* 20 → 10 ms，控制频率 100Hz */
```

**`ROBOT_INTERPOLATION_TIME_RESOLUTION` 暂时保持 100ms 不变。** 原因：它决定每个路径点的运动时长，改它会改变末端运动速度（当前 1mm/100ms = 10mm/s）。本步骤只提升控制频率（每路径点的 PID 次数从 5 次→10 次），不改运动速度。S 曲线（4.8）时再统一调整。

##### 步骤 5：积分系数减半 `APP/robot.h`

周期减半后，`intg_error` 每秒累加次数翻倍，为保持积分作用等效，Ki 减半：

```c
#define ROBOT_PID_KI                        (0.001f)/* 0.002 → 0.001，周期减半后保持积分等效 */
```

> Kd=0 不受周期影响，无需改。Kp 不变。

---

**验证要点（codex 实现后需实测）：**
1. 用串口日志测单周期耗时：在 `robot_pid_one_period` 首尾打 `xTaskGetTickCount()` 差值，确认 < 10ms。
2. 跑 `auto 15 0 0`（+X 方向，已知可解），观察电机运动是否更平滑、`ave_error` 是否下降。
3. 若单周期偶尔 > 10ms（CAN 偶发丢包重试），属正常，`vTaskDelay` 会被跳过、下周期自动追上，不会累积。

**回滚方案：** 若 100Hz 下电机抖动或丢步，将 `ROBOT_PID_PERIOD` 改回 20、`ROBOT_PID_KI` 改回 0.002、`WaitAllMotors` 超时改回 15 即可恢复。


#### 4.5 CAN 通信并行化（9.5）
当前 J1~J5 角度读取串行执行（5次请求-应答），每次约 8ms，总耗时 ~40ms，严重超过 20ms 控制周期。

改为：连续发出所有读取请求，统一等待所有回包，总耗时降至 ~6ms。

**参考来源：** `MechanicalArm_Code_V4-main/Cpp/Src/NewBee.cpp` L166-197，`Core/Src/can.c` L280-333

**原理：** MechanicalArm 使用 per-motor 独立缓冲区 + 完成标志位，ISR 按 `motor_id = ExtId >> 8` 分发到对应缓冲区。上层连续发出 6 条读命令后 while 轮询所有 flag（6ms 超时）。

**修改文件及内容：**

##### 步骤 1：`USER_BSP/bsp_can.h` — 扩展 CAN_Context_t

```c
typedef struct {
    CAN_RxHeader_Mock_t CAN_RxMsg;
    uint8_t rxData[32];
    uint8_t txData[32];
    SemaphoreHandle_t can_rx_sem;

    /* ---- 并行读取支持（per-motor 独立缓冲） ---- */
    volatile uint8_t  motor_rx_buf[6][8];   /* 每电机独立接收数据 */
    volatile uint8_t  motor_rx_dlc[6];      /* 每电机接收 DLC */
    volatile uint8_t  motor_rx_flag[6];     /* 每电机接收完成标志（ISR 置 1） */
} CAN_Context_t;
```

##### 步骤 2：`USER_BSP/bsp_can.c` — ISR 增加分发逻辑

在 `canfd0_callback` 的 `CAN_EVENT_RX_COMPLETE` 分支中，保留原有信号量逻辑后追加：

```c
/* 并行模式：按电机 ID 存入独立缓冲区 */
uint8_t motor_id_par = (uint8_t)(p_args->frame.id >> 8);
if (motor_id_par >= 1u && motor_id_par <= 6u) {
    uint8_t idx = motor_id_par - 1u;
    uint8_t clen = (copy_len > 8u) ? 8u : copy_len;
    for (uint8_t k = 0; k < clen; k++) {
        g_can_context.motor_rx_buf[idx][k] = p_args->frame.data[k];
    }
    g_can_context.motor_rx_dlc[idx] = clen;
    g_can_context.motor_rx_flag[idx] = 1u;
}
```

##### 步骤 3：`USER_BSP/bsp_can.c` — 新增批量等待 API

```c
/* 清除所有电机接收标志（并行读取前调用） */
void BSP_CAN_ClearMotorFlags(void)
{
    for (int i = 0; i < 6; i++) {
        g_can_context.motor_rx_flag[i] = 0u;
    }
}

/* 等待指定数量电机的回包全部到达（超时返回 false） */
bool BSP_CAN_WaitAllMotors(uint8_t joint_num, uint32_t timeout_ms)
{
    TickType_t deadline = xTaskGetTickCount() + pdMS_TO_TICKS(timeout_ms);
    while (xTaskGetTickCount() < deadline) {
        bool all_done = true;
        for (uint8_t i = 0; i < joint_num; i++) {
            if (!g_can_context.motor_rx_flag[i]) {
                all_done = false;
                break;
            }
        }
        if (all_done) return true;
        taskYIELD();
    }
    return false;
}
```

##### 步骤 4：`APP/robot.c` — 新增 `robot_update_all_angles()`

```c
static int robot_update_all_angles(int joint_num)
{
    if (!BSP_CAN_Lock(200)) return 1;

    BSP_CAN_ClearMotorFlags();

    /* 连续发出所有读位置命令（不等回复） */
    for (int i = 0; i < joint_num; i++) {
        Emm_V5_Read_Sys_Params((uint8_t)(i + 1), S_CPOS);
    }

    /* 统一等待所有回包（6ms 超时，与 MechanicalArm 一致） */
    BSP_CAN_WaitAllMotors((uint8_t)joint_num, 6);

    BSP_CAN_Unlock();

    /* 解析各电机角度 */
    for (int i = 0; i < joint_num; i++) {
        if (!g_can_context.motor_rx_flag[i]) continue;

        uint8_t *rx = (uint8_t *)g_can_context.motor_rx_buf[i];
        uint8_t dlc = g_can_context.motor_rx_dlc[i];
        if (dlc < 7u || rx[0] != 0x36u || rx[dlc - 1u] != 0x6Bu) continue;

        float angle = 0.0f;
        for (int k = 5; k >= 2; k--) {
            angle += (float)(((uint32_t)rx[k]) << ((5 - k) << 3));
        }
        if (rx[1] == 0x01u) angle = -angle;
        if (g_robot.joints[i].postive_direction == MOTOR_DIR_CCW) angle = -angle;

        angle = angle * 360.0f / 65536.0f / g_robot.joints[i].reduction_ratio
                + g_joints_init[i].current_angle;
        g_robot.joints[i].current_angle = robot_angle_normalize(angle);
    }
    return 0;
}
```

##### 步骤 5：`APP/robot.c` — 修改 `robot_pid_one_period()`

将原来的逐关节 `robot_update_current_angle(j)` 循环替换为一次性批量读取：

```c
static void robot_pid_one_period(float *target_angle, float *intg_error,
                                 float *pre_error, float *total_error, int joint_num)
{
    float error = 0;
    float v;
    uint32_t pid_end_time = xTaskGetTickCount() + ROBOT_PID_PERIOD;

    /* 一次性并行读取所有关节角度（~6ms） */
    robot_update_all_angles(joint_num);

    for (int j = 0; j < joint_num; j++) {
        g_current_angle[j] = g_robot.joints[j].current_angle;
        error = robot_angle_diff(g_robot.joints[j].current_angle, target_angle[j]);
        intg_error[j] += error;
        if (total_error != NULL) {
            total_error[j] += fabsf(error);
        }
        v = ROBOT_PID_KP * error + ROBOT_PID_KI * intg_error[j] + ROBOT_PID_KD * (error - pre_error[j]);
        pre_error[j] = error;
        robot_joint_veloccity_to((uint32_t)j, v, ROBOT_JOINT_DEFAULT_ACCELERATION);
    }

    uint32_t now = xTaskGetTickCount();
    if (now < pid_end_time) {
        vTaskDelay(pid_end_time - now);
    }
}
```

**注意事项：**
- 保留原有 `robot_update_current_angle()` 不删，soft_reset/单关节操作仍需串行请求-应答
- ISR 中原有的 binary semaphore 逻辑保持不变，兼容串行模式
- `BSP_CAN_WaitAllMotors` 中使用 `taskYIELD()` 而非 `vTaskDelay(1)` 以获得最快响应

---

### P2 — 中期（提升运动质量）

#### 4.6 逆运动学分支锁定（9.4）
当前逆解选优只比较加权绝对差，没有锁定上一时刻的解分支，路径经过分支边界时关节角可能突变。

**参考来源：** `MechanicalArm_Code_V4-main/Cpp/Src/Inverse_Kinematics.cpp` L832-903，`Cpp/Inc/Inverse_Kinematics.h` L63-67

**MechanicalArm 方法：**
- 记录上一帧选中的解索引 `last_best_index`
- 选解时，对非上次解的候选乘以 `SWITCH_PENALTY = 10.0` 的惩罚系数
- 使用加权**平方**距离（而非绝对值），放大大偏差解的代价
- 角度差归一化到 `[-π, π]` 避免 ±2π 等价性干扰
- 权重：`{20.0, 15.0, 12.0, 5.0, 3.0, 1.0}`（近基座关节权重大）

**当前 Robot_FreeRTOS 实现（`robot_kinematics_get_optimal_result`）的不足：**
1. 只比较加权绝对差，无平方项 → 对大偏差不够敏感
2. 没有 `last_best_index` → 路径经过对称分支边界时来回跳
3. 权重 `{5, 3, 3, 1, 1, 0}` 相对保守

**修改位置：** `Middle/robot_kinematics.c`，`robot_kinematics_get_optimal_result()`

**实现方案：**

```c
/* 新增文件作用域变量 */
static int g_last_best_index = -1;
static const float IK_SELECT_WEIGHTS[6] = {20.0f, 15.0f, 12.0f, 5.0f, 3.0f, 1.0f};
static const float IK_SWITCH_PENALTY = 10.0f;

static int robot_kinematics_get_optimal_result(volatile float *result)
{
    float min_dist = FLT_MAX;
    int best_index = -1;

    for (unsigned int i = 0; i < ROBOT_KINEMATICS_RESULT_NUM; i++) {
        if (g_robot_kinematics.result_invalid_mask & (1u << i)) {
            continue;
        }

        float dist = 0.0f;
        for (unsigned int j = 0; j < ROBOT_MAX_JOINT_NUM; j++) {
            float diff = g_robot_kinematics.result[i][j] - g_current_joint_angle[j];
            /* 角度差归一化到 [-180, 180] */
            while (diff > 180.0f) diff -= 360.0f;
            while (diff < -180.0f) diff += 360.0f;
            dist += IK_SELECT_WEIGHTS[j] * (diff * diff);
        }

        /* 分支锁定：非上次选中的解施加惩罚 */
        if (g_last_best_index != -1
            && !(g_robot_kinematics.result_invalid_mask & (1u << (unsigned)g_last_best_index))
            && (int)i != g_last_best_index) {
            dist *= IK_SWITCH_PENALTY;
        }

        if (dist < min_dist) {
            min_dist = dist;
            best_index = (int)i;
        }
    }

    if (best_index == -1) {
        return -1;
    }

    g_last_best_index = best_index;

    for (unsigned int j = 0; j < ROBOT_MAX_JOINT_NUM; j++) {
        result[j] = g_robot_kinematics.result[best_index][j];
    }
    return 0;
}
```

**关键改动点：**
1. 加权绝对差 → 加权**平方**差（`diff * diff`）
2. 新增 `g_last_best_index` 记录上一帧解索引
3. 非上次解乘以 10 倍惩罚
4. 角度差归一化到 `[-180°, 180°]`
5. 权重更新为 MechanicalArm 的 `{20, 15, 12, 5, 3, 1}`

**注意事项：**
- `g_last_best_index` 在 soft_reset 完成后应重置为 -1（防止复位后锁定到错误分支）
- 原有 `joint_weight` 数组可保留用于其他场景，IK 选解使用新的 `IK_SELECT_WEIGHTS`

#### 4.7 奇异点三次阻尼（9.4）
当前 θ5 接近 0/π 时直接令 θ4=0，穿越奇异点时关节角突变。

参考 MechanicalArm：使用三次方阻尼曲线冻结 J4，由 J6 补偿旋转量。

**修改位置：** `Middle/robot_kinematics.c`，θ4/θ5/θ6 求解部分

---

### P3 — 长期（运动平滑性根本改善）

#### 4.8 S 曲线轨迹规划（9.1）
当前线性插值在启停时速度突变，是运动不平滑的根本原因。

替换为正弦 S 曲线速度剖面（来自 MechanicalArm）：
- 长距离（S ≥ 283.53mm）：加速 + 匀速 + 减速（三段式）
- 短距离（S < 283.53mm）：加速 + 减速（两段式）
- 速度公式：`v(t) = (Vmax/2)·sin(Ω·t - π/2) + Vmax/2`
- 参数：Vmax=300mm/s，Amax=500mm/s²，Ω=3.333rad/s

**前提：** 必须先完成 4.4（100Hz）和 4.5（CAN 并行化）。

**修改位置：** `APP/robot.c`，替换 `robot_path_interpolation_linear()`

#### 4.9 位置模式到位确认（9.7）
`robot_joint_rotate_to()` 发送位置指令后立即更新角度，不等待电机实际到位，误差累积。

增加到位轮询函数，在单关节运动后确认实际角度。

---

## 5. 实施顺序

```
第一阶段（当前）
    ├── 4.1 工程裁剪（重做）
    ├── 4.2 修复初始化逻辑反转（重做）
    └── 4.3 控制器替换为前馈+P（重做）

第二阶段
    ├── 4.5 CAN 并行化
    └── 4.4 控制频率提升至 100Hz（依赖 4.5）

第三阶段
    ├── 4.6 IK 分支锁定
    └── 4.7 奇异点三次阻尼

第四阶段
    ├── 4.8 S 曲线轨迹规划（依赖 4.4 + 4.5）
    └── 4.9 位置模式到位确认
```

---

## 6. 关键参数速查

| 参数 | 当前值 | 目标值 | 说明 |
|------|--------|--------|------|
| 控制周期 | 20ms | 10ms | 完成 CAN 并行化后修改 |
| 路径点间隔 | 100ms | 10ms | 与控制周期同步修改 |
| 空间插值精度 | 1mm | 1mm（保持） | S 曲线后由速度决定间距 |
| Kp（全关节） | 10.0 | {0.65,0.50,0.75,1.00,1.00,10.0} | 各关节独立 |
| Ki | 0.002 | 0（去掉） | 前馈+P 无积分 |
| CAN 超时 | 15ms | 15ms（保持） | 并行化后总耗时降至 ~6ms |
