 

# Robot_FreeRTOS-Arm 机械臂控制文档

> 本文档以当前工作区代码为事实来源，记录机械臂控制架构、已完成改动、剩余任务和验证状态。
> 更新时间：2026-06-

---

## 1. 工程范围

当前主要启动路径包括机械臂控制任务、P801 激光初始化和视觉定靶线程：

```text
robot_thread_entry
    -> LED_Init
    -> BSP_Laser_Init
    -> BSP_UART_Init
    -> BSP_CAN_Init
    -> robot_init
        -> robot_control_task
        -> robot_cmd_service
vision_service_thread_entry
    -> jetson_vision_init
    -> robot_target_init
    -> 10 ms 周期运行 jetson_vision_process + robot_target_step
```

机械臂运动学控制 J1~J5，J6 由夹爪模块独立控制。`vision_service_thread` 当前负责 Jetson 视觉误差解析和激光定靶状态机，不再驱动旧视觉采摘流程。

工程中已移除旧 `K230_cmd` 和 `robot-take` 编译项，改为 `jetson_vision` 和 `robot_target`。底盘、DHT11、MQTT、W800 等外围模块仍保留，因此 4.1 工程裁剪仍是部分完成。

---

## 2. 当前控制架构

### 2.1 事件与任务

UART/MQTT 命令进入 `g_robot.cmd_queue`，由 `robot_cmd_service` 解析后发送控制事件；`robot_control_task` 串行处理机械臂事件。

主要事件包括：

- 单关节相对/绝对旋转
- 自动笛卡尔运动
- 时间函数轨迹
- soft reset / hard reset
- remote 连续控制
- J1~J5 批量角度读取

AUTO 在当前位姿无效时会被拒绝。反馈读取异常但仍可继续时，系统使用 `ROBOT_STATUS_POSE_DEGRADED` 标记退化状态。

### 2.2 AUTO 运动流程

```text
目标位移
    -> 10 ms 正弦速度 S 曲线笛卡尔插值
    -> 每个路径点进行逆运动学求解
    -> IK 分支锁定选择连续解
    -> 每 10 ms 更新一个关节目标节点
    -> 每个路径点执行一次前馈 + P 控制
    -> 终点保持 10 个控制周期，前馈清零并由 P 项收敛
    -> J1~J5 非阻塞发送速度命令
    -> 结束后批量停止 J1~J5
```

路径和逆解结果使用静态缓冲区：

```c
#define ROBOT_MAX_PATH_SIZE (600)

static struct position s_path_buf[ROBOT_MAX_PATH_SIZE];
static float s_result_buf[ROBOT_MAX_PATH_SIZE * ROBOT_MAX_JOINT_NUM];
```

路径最多保存 600 点；S 曲线达到容量上限时会把最后一点强制替换为目标点。AUTO 和时间函数运动不再在运行时分配路径/逆解内存。

### 2.3 前馈 + P 控制器

控制周期为 10 ms（100 Hz），控制器结构为：

```c
error = shortest_angle_diff(current_angle, target_angle);
velocity = ROBOT_JOINT_FF_GAIN[joint] * feedforward
         + ROBOT_JOINT_KP[joint] * error;
velocity = clamp(velocity, -200.0f, 200.0f);
```

各关节 Kp（单位 `1/s`）：

```c
{0.65f, 4.00f, 3.00f, 2.00f, 2.30f, 10.0f}
```

控制器无积分项和微分项，不存在原 PID 的积分饱和问题。速度前馈使用独立系数 `ROBOT_JOINT_FF_GAIN[]` 做逐关节缩放，见 4.9.6。

前馈时间基准按目标更新周期计算：

- AUTO：S 曲线每 10 ms 生成一个目标点，相邻 IK 节点角差除以 10 ms，再按 `ROBOT_JOINT_FF_GAIN[]` 做逐关节缩放。
- 时间函数：同样按 10 ms 生成目标点，但路径由时间函数直接给出，不经过 S 曲线规划器；速度前馈同样进入 `robot_pid_one_period()` 并经过 `ROBOT_JOINT_FF_GAIN[]` 缩放。
- remote：相邻目标角差除以 50 ms；首次更新前馈为 0，避免进入模式时产生冲击；当前也复用 `robot_pid_one_period()`，因此前馈同样经过 `ROBOT_JOINT_FF_GAIN[]` 缩放。
- 角差通过 `robot_angle_diff()` 归一化到 `[-180, 180]`，避免跨越 0/360 度时方向错误。

`ave_error` 使用实际控制采样次数作为分母，表示各关节的平均绝对跟踪误差，不再按路径点数错误放大。

### 2.4 CAN 反馈与控制输出

每个控制周期采用并行反馈读取：

1. 清除 `motor_rx_flag` 和残留 RX 信号量。
2. 连续发送 J1~J5 位置读取请求。
3. CAN ISR 按电机地址将 `0x36` 回包写入独立缓存并置位。
4. `BSP_CAN_WaitAllMotors()` 统一等待，当前批量等待上限为 6 ms。
5. 解析已收到的回包并更新关节角度。

速度控制使用 `robot_joint_velocity_nowait()`：发送 `0xF6` 后立即返回，不逐关节等待应答。CAN 总线仍由互斥锁保护，避免与夹爪、单关节读取等操作互相干扰。

停止操作使用 `robot_joint_stop_all()`：连续发送所有 stop 命令，再通过 `motor_stop_flag` 统一等待 `0xFE` 回包。

`BSP_CAN_DrainRx()` 当前负责清空 RX 信号量，避免残留广播帧或旧通知消耗下一次请求的应答窗口。

### 2.5 复位与位姿有效性

soft reset 当前流程：

1. 读取并映射各关节当前角度。
2. 发送位置模式回 HOME 命令。
3. 根据初始角度误差估算行进时间，并增加 300 ms 余量。
4. 每 40 ms 轮询反馈，最少等待 320 ms，最多等待 3 s。
5. 校验 HOME；失败时执行最多 3 个 refine pass。
6. 每个关节每个 pass 最多微调 3 轮，容差为 1.0 度。
7. 最终校验通过后设置 HOME 位姿；失败则清除 `POSE_VALID` 并进入退化状态。

`robot_angle_map()` 对距离 min/max 不超过 0.1 度的读数直接夹紧到边界，避免边界噪声被错误卷绕到另一圈。soft reset 映射还提供额外 1 度边界容差。

当前 J2/J3/J5 的软件限位相对 HOME 留有 2 度余量：J2 下限 88 度、J3 下限 -92 度、J5 上限 92 度。

### 2.6 IK 连续性

逆运动学已经实现分支锁定：

- 使用加权平方角度距离选择最接近当前姿态的有效解。
- 权重为 `{20, 15, 12, 5, 3, 1}`。
- 对非上一帧分支乘以 `IK_SWITCH_PENALTY = 10`，降低路径中途跳解概率。
- 新 AUTO、时间函数、remote 和复位完成后调用 `robot_kinematics_reset_branch_lock()`，首帧重新从实际姿态选择最近解。
- J6 不参与整臂轨迹驱动，AUTO/时间函数保持进入运动时的 J6 角度。

当前只处理了精确奇异点的基础分支，尚未实现 4.7 的奇异区连续阻尼。

---

## 3. 关节参数


| 关节 |   HOME |  软件限位 | 减速比 | 正方向 | 回零方向 |
| ------ | -------: | ----------: | -------: | -------- | ---------- |
| J1   |  90 度 |  0~360 度 |  50.00 | CCW    | 负方向   |
| J2   |  90 度 | 88~180 度 |  99.99 | CW     | 负方向   |
| J3   | -90 度 | -92~90 度 |  50.89 | CW     | 负方向   |
| J4   |   0 度 | -90~90 度 |  51.00 | CW     | 负方向   |
| J5   |  90 度 |   0~92 度 |  26.85 | CCW    | 正方向   |
| J6   |   0 度 |  0~360 度 |  51.00 | CW     | 负方向   |

J4 使用 `JOINT_LIMIT_6`，J6 使用 `JOINT_LIMIT_4`，这是当前硬件接线映射，不按名称交换。

---

## 4. 改进项状态


| 编号 | 改进项                 | 当前状态 | 代码事实                                                                                             |
| ------ | ------------------------ | ---------- | ------------------------------------------------------------------------------------------------------ |
| 4.1  | 工程裁剪               | 部分完成 | K230/robot-take 旧视觉采摘链路已替换为 Jetson/robot_target；底盘、DHT11、MQTT、W800 等外围模块仍保留 |
| 4.2  | 修复限位开关初始化判断 | 已完成   | 6 处`open()` 均使用 `!= FSP_SUCCESS` 判断失败                                                        |
| 4.3  | 前馈 + P、独立 Kp      | 已完成   | 无 Ki/Kd，节点感知前馈，输出限幅为正负 200 度/秒                                                     |
| 4.4  | 控制频率提升至 100 Hz  | 已完成   | `ROBOT_PID_PERIOD = 10 ms`，AUTO 路径节点与控制周期对齐                                              |
| 4.5  | CAN 通信并行化         | 已完成   | 批量位置反馈、ISR 分发、非阻塞速度发送和批量 stop 均已实现                                           |
| 4.6  | IK 分支锁定            | 已完成   | 加权平方距离、切换惩罚和新运动前重置均已实现                                                         |
| 4.7  | 奇异点三次阻尼         | 待完成   | 当前仅有精确奇异点基础处理，没有奇异区连续权重                                                       |
| 4.8  | S 曲线轨迹规划         | 已完成   | AUTO 使用正弦速度剖面、10 ms 变距节点、长短路径分段和终点稳定段                                      |
| 4.9  | 位置模式到位确认       | 已完成   | 单关节位置命令已增加 `robot_joint_wait_target()`；AUTO/time_func 已增加 `robot_auto_final_confirm()`，终点未确认时不再更新 `cur_pos` |
| 4.10 | Jetson 视觉误差通信    | 已完成   | `jetson_vision.c/.h` 已实现 0x03 视觉误差解析、AA/BB 启停控制帧、状态回传和 UART TX 完成回调         |
| 4.11 | 激光定靶状态机         | 已完成   | `robot_target.c/.h` 已实现 10ms 周期状态机、视觉精瞄、P000 发射许可和 P801 激光输出控制              |
| 4.12 | 末端精度与分辨率提升   | 部分完成 | 已完成两段式步长、`ROBOT_AUTO_EVENT` busy 防排队和 `TARGET_CONFIRM` 二次确认；低 Amax/延长 settle 保留为第二阶段实验项 |

### 4.7 奇异点三次阻尼

目标是在腕部接近奇异区时连续降低 J4 分量，而不是只在精确奇异点切换公式。建议继续参考 `MechanicalArm_Code_V4-main`：

- 根据 `sin(theta5)` 或等价奇异度构造连续权重。
- 奇异区外权重为 1，进入奇异区后平滑衰减到 0。
- 使用三次平滑函数保证权重及其一阶导数连续。
- 锁定或缓慢更新 J4，并由其余腕部自由度补偿。
- 上板验证 J4/J5 在奇异区附近不存在突跳。

### 4.8 S 曲线轨迹规划

AUTO 已切换到 `robot_path_interpolation_scurve()`，路径直接写入 `s_path_buf`。当前实现参考 `MechanicalArm_Code_V4-main`，按 10 ms 时间步长积分正弦速度剖面：

- `Vmax = 180 mm/s`，`Amax = 300 mm/s²`（代码常量 `SCURVE_VMAX`/`SCURVE_AMAX`，此前文档记录的 300/500 已过期，以代码为准）。
- `Omega = 2*Amax/Vmax ≈ 3.333 rad/s`，加速段时长约 0.9425 s，加速段位移约 84.83 mm。
- 路径不短于约 169.65 mm 时使用“加速 + 匀速 + 减速”三段式。
- 短路径使用“加速 + 减速”两段式，并根据距离降低峰值速度。
- 每步按 `s += v(t)*0.01` 生成变距笛卡尔目标，并将最后一个点强制设为精确目标。
- 最大生成 800 点；达到容量上限时最后一点仍会被替换为目标点。
- `robot_pid_run()` 每个目标点只执行一次 10 ms 控制周期，前馈使用相邻关节目标角差除以 10 ms。
- 轨迹结束后追加 10 个控制周期的终点稳定段：前馈清零，只保留 P 项收敛，然后批量停止电机。
- MQTT 关节同步改为每 10 个路径点执行一次，约 100 ms 一次。

代码实现已完成，仍需重点验证：

1. `auto 0 -70 0` 的启停平滑性和各关节 `ave_error`。
2. `auto 0 -140 0` 等长路径不触发 600 点截断。
3. `auto 0 0 100` 不产生 IK 失败或前馈限幅持续饱和。
4. 近零位移当前只生成 1 个路径点，而 `robot_pid_run()` 从索引 1 开始处理；进入末端稳定段前目标数组可能仍为零值，该边界必须修复并回归验证。
5. 如果路径达到 600 点上限，最后一点被强制替换为目标，需确认不会形成末端大步跳变。

### 4.9 位置模式到位确认

本节原本只覆盖单关节命令，实测数据显示 AUTO/笛卡尔路径的到位确认缺口更严重，因此拆成两个子问题一起解决，避免各自打补丁形成两套不一致的边界逻辑。

#### 4.9.1 问题一：单关节绝对/相对命令不确认到位

现状：`ROBOT_JOINT_REL_ROTATE`/`ROBOT_JOINT_ABS_ROTATE` 最终都落到 `robot_joint_rotate_to()`（`robot.c:239-304`），该函数只计算步数/速度并调用 `Emm_V5_Pos_Control()` 发送位置命令，随即返回，不等待、不校验电机是否真的转到目标角度。命令发出后 `g_robot.joints[j].current_angle` 要等下一次主动刷新才会更新，期间上层认为该关节已经在目标角度。

#### 4.9.2 问题二：AUTO/笛卡尔路径完成后同样不确认到位，且实测误差已经很大

现状核实：`auto` 命令走绝对笛卡尔目标 + 末端直线插补（`robot_path_interpolation_scurve()`）+ 固定 `T_0_6_reset` 姿态；逆解 `robot_kinematics_get_optimal_result()` 只做关节限位和最近分支选择，不做自碰检查。路径执行完毕后，`robot_auto_move_interpolation()`（`robot.c:1027-1086`）末尾：

```c
ret = robot_pid_run(path, path_size, result);
if (ret == 0) {
    g_robot.cur_pos.x = target_pos->x;
    g_robot.cur_pos.y = target_pos->y;
    g_robot.cur_pos.z = target_pos->z;
    robot_try_refresh_joints_feedback(1u);
}
robot_auto_busy_clear();
```

`robot_pid_run()` 返回 0 只代表路径插补过程中没有出现无效角度（NaN/Inf/超量程）而中止，**不代表**各关节最终收敛到目标角度内。`cur_pos` 却在这个条件下被直接写成命令目标，而不是根据关节实际到位情况确认。后续每一次新的 `auto` 都以这个可能已经漂移的 `cur_pos` 为起点重新插补，误差会跨多次调用累积。

实测数据（用户上板测试，`robot_pid_run()` 自带的 `ave_error` 日志）：

| 指令 | 关节 | ave_error |
| --- | --- | --- |
| `auto 0 -90 34` | J5 | 17.74° |
| `auto 0 -65 -1.5` | J3 | 18.77° |
| `auto 0 -65 -1.5` | J5 | 24.89° |

`ave_error` 是整条路径（含末尾 `ROBOT_PID_SETTLE_PERIODS=10` 个稳定周期）的平均绝对误差，不是终点瞬时误差，但十几到二十几度的量级足以说明这两条 AUTO 执行完之后关节远未收敛，而 `cur_pos` 仍被当作已到位处理。

#### 4.9.3 根因定位（早期假设，已被 4.9.6 修正）

> 注：本节当时把 final_confirm 反复失败归因于"固定稳定段 + 到点硬停"，并据此做了自适应稳定段改造。实测该改造反而让运动手感变差（把带偏差的 P 收敛拉长到 1.5s，创进/超调更明显），已回退为固定短窗口。真正根因见 **4.9.6：AUTO 速度前馈偏大导致比例型跟踪偏差**。本节保留作为排查记录。

现象：4.9.4 上线后，`robot_auto_final_confirm()` 频繁报"未到位"，但失败的关节并不固定——同一份 `debug.txt` 里第一条 `auto` 报 J3 err=20.86°，第二条 `auto` 换成报 J2 err=16.01°。此前按"哪个关节报错就上调哪个关节 Kp"的方式处理（J5 1.0→4.5，J4 1.0→3.5），结果只是把瓶颈转移到下一个关节，没有解决问题——这说明问题不在某个关节的增益不够，而在触发确认之前的机制本身。

根因：`robot_pid_run()`（`robot.c:1246-1299`，改造前）末端稳定段是固定周期数：

```c
for (int k = 0; k < ROBOT_PID_SETTLE_PERIODS; k++) {   /* 固定 10 个周期 = 100ms */
    robot_pid_one_period(target_angle, feedforward, total_error, ROBOT_ARM_JOINT_NUM);
    sample_count++;
}
robot_joint_stop_all(ROBOT_ARM_JOINT_NUM);              /* 到时间立即硬停，不管是否收敛 */
```

控制律只有前馈 + P 项、没有 I 项（当前 `robot_pid_one_period()`：`v = ROBOT_JOINT_FF_GAIN[j] * feedforward[j] + ROBOT_JOINT_KP[j] * error`）。进入末端稳定段后前馈被清零，只剩 P 项按当前 Kp 收敛；固定 100ms 稳定段到时间就 `robot_joint_stop_all()` 硬停——不管当前误差多大，直接冻结在当时的位置。`robot_auto_final_confirm()` 之后只是轮询这个已经停转的关节，且本身不发任何纠正指令（对照 `robot_soft_reset_refine_joint()` 会在未收敛时重新下发旋转指令主动纠正），所以一旦硬停时还没收敛，接下来 800ms 的确认窗口只会原地重复读到同一个超差角度，必然判定失败。

哪个关节先"撞上"这个 100ms 硬停线，取决于该关节在这条具体路径里此刻的残余误差和自身时间常数——不同目标点、不同起始姿态，各关节残余误差的分布不同，所以失败关节每次不一样。持续上调某一关节 Kp 只是让它在这条路径里不再是最慢的，下一条路径换成另一个关节垫底，这正是用户观察到的"改一个位置，下一个位置又不行"。

结论：这是稳定段"固定时长 + 到点硬停"的结构性缺口，不是任何单个关节的增益问题，继续调 Kp 不能收敛到通用解，且盲目调高还会推高 J3/J4/J5 的振荡风险。

#### 4.9.4 当前实现

4.9 已按最小路径实现，两个子问题共用同一套角度误差判定：读取真实角度 → `robot_joint_compare_error()` 映射并计算误差 → 连续 2 次命中才认为到位。当前参数：

| 参数 | 当前值 | 说明 |
| --- | ---: | --- |
| `ROBOT_JOINT_POS_CONFIRM_TOL_DEG` | `3.0f` | 到位确认角度容差 |
| `ROBOT_JOINT_POS_CONFIRM_STABLE_HITS` | `2` | 连续命中次数 |
| `ROBOT_JOINT_POS_CONFIRM_PERIOD_MS` | `40` | 轮询周期 |
| `ROBOT_JOINT_POS_CONFIRM_TIMEOUT_MS` | `3000` | 单关节位置命令确认超时 |
| `ROBOT_AUTO_FINAL_CONFIRM_TIMEOUT_MS` | `800` | AUTO/time_func 终点确认超时 |
| `ROBOT_PID_SETTLE_PERIODS` | `10` | 末端固定稳定段，前馈清零后按 10 ms 周期合计约 100ms |

- **单关节/关节同步命令**：`ROBOT_JOINT_REL_ROTATE` / `ROBOT_JOINT_ABS_ROTATE` 调用 `robot_joint_rotate_to()` 发出位置命令后，立即用 `robot_joint_wait_target()` 轮询真实角度；确认失败时调用 `robot_joint_stop()` 停止对应关节，并打印 `joint ... position confirm timeout`。当前 `ROBOT_JOINTS_SYNC_EVENT` 仅批量下发 J1~J5 目标角并更新软件角度，没有逐关节等待确认。
- **AUTO/time_func**：`robot_pid_run()` 返回 0 后不再直接写 `g_robot.cur_pos`。必须先通过 `robot_auto_final_confirm()` 批量读取 J1~J5 实际角度并确认最终目标角；全部到位后才更新 `cur_pos`，否则清除 `ROBOT_STATUS_POSE_VALID`、置位 `ROBOT_STATUS_POSE_DEGRADED`，后续 `auto` 会被拒绝，要求重新 `soft_reset` 建立可信位姿。
- **末端稳定段保持固定短窗口**：当前可用版本保留 `ROBOT_PID_SETTLE_PERIODS=10`，即前馈清零后再执行约 100ms P 控制稳定段，然后停止并进入 `robot_auto_final_confirm()`。本轮不引入自适应稳定段，避免再次影响现有运动手感。
- **保留项**：4.9 位置确认机制本身不负责调 Kp、不改 IK、不加自碰检测；当前 Kp 调整记录见 2.3 和参数速查表。若修复自适应稳定段后仍有关节持续无法在 1.5s 内收敛到 3°，才需要回到该关节的 Kp/机械负载问题，而不是反过来先调 Kp。4.9 只解决”位置命令是否真的到位”和”未到位时不能把软件位置写成目标点”。

#### 4.9.5 三处补强（本轮，均为最小改动，不扩大 4.9 范围）

1. **`robot_pid_run()` 异常返回时同样要清 `POSE_VALID`**：`robot_auto_move_interpolation()` 和 `robot_time_func_move()` 原来的判断是 `if (ret == 0) { ... }`，`ret != 0`（路径插补中途读到 NaN/Inf/超量程角度而中止）时什么都不做——机械臂在中止前可能已经按前面几个路径点转动了一部分，但软件侧的 `ROBOT_STATUS_POSE_VALID` 还停留在上一次成功的状态，等于用旧的可信状态掩盖了本次的中途失败。现在补上 `else` 分支，和 final confirm 失败走同一处理：清 `ROBOT_STATUS_POSE_VALID`、置 `ROBOT_STATUS_POSE_DEGRADED`，强制下一次 `auto` 前必须先 `soft_reset` 重新建立可信位姿。改动位置：`robot.c` 两处 `robot_pid_run(path, path_size, result)` 调用之后的 `if (ret == 0) {...}`，各加一个 `else`，不改 `robot_pid_run()` 本身、不改 final confirm 逻辑。

2. **4.9 到位确认 ≠ 自碰撞/工作空间检查**：`auto 0 -90 34` 到 `auto 0 -65 -1.5` 之间出现的自碰是逆解 `robot_kinematics_get_optimal_result()` 选出的姿态本身不安全（没有做自碰检测和工作空间限制），4.9 校验的是”关节是否转到了指令要求的角度”，指令角度本身安全与否不在 4.9 的判断范围内——就算所有关节都精确到位，只要指令角度选错了，一样会碰撞。这类问题需要在逆解或路径生成层单独加工作空间限制/危险姿态过滤，属于独立的后续项，本轮不实现，仅在此明确边界，避免以后误以为”到位确认覆盖了安全性”。

3. **`ROBOT_JOINT_POS_CONFIRM_TOL_DEG=3.0°` 是当前阶段的稳定值，不是最终精度目标**：3° 是为了不让正常收敛范围内的关节被误判为失败而选的宽松容差。等 4.9.6 的前馈系数配合当前 Kp/负载/速度稳定运行、确认不再出现结构性误判后，可以按实测再收紧到 2°甚至 1.5°，作为独立的精度调优任务，不在本轮一起改。

#### 4.9.6 真正根因与修复：AUTO 速度前馈偏大导致比例型跟踪偏差（本轮）

**定位过程（已把根因范围锁死）**：单点位置模式 `abs_rotate` 能精准到位——`auto 0 -30 0` 逆解出的目标角 J2=97.04°/J3=-81.25°/J5=88.29°，用 `abs_rotate` 逐关节下发都能准确到达。这说明：IK 目标角没错、角度映射/减速比/限位没错、`AUTO final confirm failed` 不是误判。问题只在 `auto` 的速度闭环执行链路，即 `robot_pid_run()` 进入 `robot_pid_one_period()` 后的 `ROBOT_JOINT_FF_GAIN[j] * feedforward[j] + Kp*error`。

**根因**：AUTO 的速度前馈偏大，实际电机跑得比 S 曲线规划预期更远，偏差随移位比例放大——这是**比例型跟踪偏差**，不是单点 Kp 问题：

| 命令 | 目标 J3 | 实际 J3 | 偏差 |
| --- | ---: | ---: | ---: |
| `auto 0 -30 0` | -81.25° | -73.52° | 7.73° |
| `auto 0 -50 0` | -75.28° | -62.30° | 12.98° |

偏差随目标移位增大而近似成比例放大，正是前馈项（∝ 速度）系数偏大的特征。此前反复上调单关节 Kp 只是在补偿这个偏差，且把瓶颈在关节间来回搬，无法收敛到通用解。

**修复（最小改动，不碰 HOME/IK/限位/位置模式/soft_reset）**：给每个关节加独立前馈系数，速度律改为 `v = ROBOT_JOINT_FF_GAIN[j] * feedforward[j] + ROBOT_JOINT_KP[j] * error`。对偏差最大的 J2/J3/J5 降前馈，其余 1.0：

```c
static const float ROBOT_JOINT_FF_GAIN[ROBOT_MAX_JOINT_NUM] =
    {1.0f, 0.70f, 0.55f, 1.0f, 0.60f, 1.0f}; /* J1..J6 */
```

同时把 `robot_auto_final_confirm()` 失败日志从"只打印 worst joint"改为逐关节打印 `cur/target/err`，供下一轮按实测数据细调前馈系数。改动位置：`robot.c` — `ROBOT_JOINT_FF_GAIN[]` 声明（紧接 `ROBOT_JOINT_KP[]`）、`robot_pid_one_period()` 速度律一行、`robot_auto_final_confirm()` 失败分支的逐关节 LOG。

**为什么这样能同时满足"AUTO 连续"和"误差可优化"**：
- **连续**：前馈系数只缩放已有速度项，不改路径生成、不加阻塞、不改状态机；配合 4.9.5-1 的 `POSE_VALID` 补强，单条 `auto` 到位后位姿可信，下一条 `auto` 无需人工 `soft_reset` 即可衔接，即为"连续"。若某条 `auto` 中途异常/未到位，则按 4.9.5-1 置 `POSE_DEGRADED` 主动打断，要求 `soft_reset` 后再续——这是安全的连续，不是带着未知偏差硬接。
- **误差可优化**：前馈系数把误差来源从"关节间互相掩盖的 Kp"变成"每关节一个可独立标定的比例系数"，配合逐关节 `cur/target/err` 日志，形成"跑 `auto` → 读每关节 err → 调对应 `FF_GAIN[j]`"的闭环。这是可收敛的调参路径，而不是之前"改一个位置下一个又不行"的打地鼠。

**调参路线（逐轮，不在本轮一次改完）**：
1. 用当前系数跑 `auto 0 -30 0`、`auto 0 -50 0` 等，读失败日志里每关节 `err`。
2. 某关节仍系统性偏小（实际没跑到目标）→ 调高其 `FF_GAIN[j]`；系统性超调 → 调低。J3 现已偏超调，优先在 0.5~0.6 区间微调，不要优先加 J3 的 Kp（加 Kp 更易抖/超调）。J2 从 0.70、J5 从 0.60 起步。
3. 各关节偏差稳定进入 3° 内、且不同目标点都稳定后，再考虑把确认容差从 3° 收紧到 2°/1.5°（见 4.9.5-3）。
4. **保留项（本轮不做）**：自碰撞/工作空间限制（见 4.9.5-2）、给 Kp/前馈加积分项消除稳态残差、视觉跟随。均为独立后续项，不塞进 4.9。

### 4.10 Jetson 视觉误差通信（当前实现）

#### 当前代码位置


| 文件                                | 当前职责                                                                                            |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `Middle/jetson_vision.c`            | Jetson UART 初始化、DMA/DTC 环形接收读取、视觉误差帧和启停控制帧解析、RA6M5 状态回传                |
| `Middle/jetson_vision.h`            | 视觉协议常量和对外 API                                                                              |
| `USER_BSP/bsp_uart.c`               | Jetson UART TX complete 中断回调转发到`jetson_notify_tx_complete_from_isr()`                        |
| `src/vision_service_thread_entry.c` | 10 ms 周期调用`jetson_vision_process()`，先消费启停控制帧，再把新视觉误差交给 `robot_target_step()` |

Jetson UART 对应 FSP 生成实例 `robot_jeston_ctrl`、`robot_jeston_cfg` 和 `g_transfer_jeston_rx`。业务代码层已经不再使用 `K230_cmd`、`k230_get_*`、`k230_send_*`、`K230_FUNC_*` 等旧接口。

#### Jetson -> RA6M5 协议

当前只识别一种视觉误差帧：

```text
[0xFF][0x05][0x03][dcx_lo][dcx_hi][dcy_lo][dcy_hi][checksum][0xFE]
```


| 字段     |     值 | 说明                                       |
| ---------- | -------: | -------------------------------------------- |
| SOF      | `0xFF` | 帧起始                                     |
| LEN      | `0x05` | `FUNC(1) + DATA(4)`                        |
| FUNC     | `0x03` | `JETSON_FUNC_VISION_ERROR`，视觉误差帧     |
| DATA     | 4 字节 | `int16_t dcx` + `int16_t dcy`，小端        |
| CHECKSUM | 1 字节 | `LEN + FUNC + DATA...` 的 `uint8_t` 截断和 |
| EOF      | `0xFE` | 帧结束                                     |

解析状态机只接受 `SOF=0xFF`、`LEN=0x05`、`FUNC=0x03`、4 字节 payload、匹配 checksum 和 `EOF=0xFE`。旧 K230 的 `0x02` 草莓检测、`0x04` 可采摘判断、`0x06` 数量帧已经删除，不再被解析。

`dcx/dcy` 含义：

- `dcx`：画面 X 轴像素偏差，正方向由 Jetson 端定义，当前控制侧按 `TARGET_KX_MM_PER_PX=-0.38f` 映射到机械臂 X 修正。
- `dcy`：画面 Y 轴像素偏差，当前控制侧按 `TARGET_KY_MM_PER_PX=0.35f` 映射到机械臂 Z 修正。

#### Jetson -> RA6M5 定靶状态机启停控制帧（当前实现）

当前在同一个 Jetson UART 接收通道中兼容 4 字节启停控制帧，不新建第二套通信模块：

```text
[0xAA][FUNC][VALUE][0xBB]
```


| 字段  | 值            | 说明                     |
| ------- | --------------- | -------------------------- |
| SOF   | `0xAA`        | 启停控制帧头             |
| FUNC  | `0x01`        | 定靶状态机控制           |
| VALUE | `0x01`/`0x00` | `0x01` 启动，`0x00` 关闭 |
| EOF   | `0xBB`        | 启停控制帧尾             |

示例：

```text
AA 01 01 BB   // Jetson 请求启动 target 状态机
AA 01 00 BB   // Jetson 请求关闭 target 状态机
```

解析策略：

- `0xFF ... 0xFE` 仍按现有视觉误差帧解析，保持 `FF 05 03 ... checksum FE` 不变。
- `0xAA 0x01 value 0xBB` 作为状态机启停帧，仅接受 `value=0x01/0x00`。
- 非 `0xFF` / `0xAA` 字节直接忽略；控制帧 `FUNC`、`VALUE` 或 `EOF` 非法时丢弃当前帧，不改变状态机。
- 启停控制帧不携带 checksum，原因是只有 4 字节、只控制布尔开关，最小实现优先；需要抗干扰增强时再增加校验。

#### RA6M5 -> Jetson 回传

状态回传格式保持 4 字节：

```text
[0xCC][FUNC][VALUE][0xDD]
```

当前回传定义如下：


|   FUNC | 宏                          |  VALUE | 当前含义                              |
| -------: | ----------------------------- | -------: | --------------------------------------- |
| `0x01` | `RA6_TO_JETSON_READY`       | `0x01` | 机械臂已到预定位，Jetson 可发送视觉帧 |
| `0x02` | `RA6_TO_JETSON_ALIGN_DONE`  | `0x01` | 连续稳定对准完成，等待输出许可        |
| `0x03` | `RA6_TO_JETSON_OUTPUT`      | `0x01` | 激光输出已进入激活状态                |
| `0x03` | `RA6_TO_JETSON_OUTPUT`      | `0x00` | 激光输出已切断                        |
| `0x04` | `RA6_TO_JETSON_TARGET_CTRL` | `0x01` | 定靶状态机已启用                      |
| `0x04` | `RA6_TO_JETSON_TARGET_CTRL` | `0x00` | 定靶状态机已关闭或启用失败            |
| `0xFE` | `RA6_TO_JETSON_ERROR`       | `0x01` | 当前实现用于限位/急停类错误上报       |

`jetson_send_status_u8()` 使用 `R_SCI_UART_Write()` 异步发送，并通过 `jetson_notify_tx_complete_from_isr()` 释放发送完成信号量，等待超时为 20 ms。

启停状态机回传示例：

```text
CC 04 01 DD   // RA6M5 回传：状态机已启用
CC 04 00 DD   // RA6M5 回传：状态机已关闭，或启动请求被拒绝后保持关闭
```

#### 对外 API

```c
void jetson_vision_init(void);
void jetson_vision_process(void);
bool jetson_get_vision_error(int16_t *dcx, int16_t *dcy);
bool jetson_get_target_control(bool *enable);
bool jetson_send_status_u8(uint8_t func, uint8_t value);
void jetson_notify_tx_complete_from_isr(void);
```

该接口和 `jetson_get_vision_error()` 一样只消费一次新命令；视觉线程在 `jetson_vision_process()` 后先消费启停命令，再组装 `target_obs_t` 运行 `robot_target_step()`。

运行方式：

1. `vision_service_thread_entry()` 启动时调用一次 `jetson_vision_init()`。
2. `jetson_vision_init()` 创建 TX 信号量、复位 parser、打开 `robot_jeston_ctrl/robot_jeston_cfg`，启动 1024 字节 RX 缓冲读取。
3. 视觉线程每 10 ms 调用 `jetson_vision_process()`。
4. `jetson_vision_process()` 通过 transfer `infoGet()` 的 `transfer_length_remaining` 计算当前写入位置，增量解析新字节；当 `remaining==0` 时解析尾段并重新启动 `R_SCI_UART_Read()`。
5. 合法视觉帧解析完成后更新 `s_latest_dcx/s_latest_dcy`，置位 `s_new_error`。
6. 合法启停控制帧解析完成后更新 `s_target_control_enable`，置位 `s_new_target_control`。
7. `jetson_get_target_control()` 只消费一次启停命令；`jetson_get_vision_error()` 只在有新帧时返回 `true`，并清除 `s_new_error`，因此上层的“连续 3 帧”判定不会重复消费同一帧。

#### 通信异常处理

当前视觉通信采用“严格接收、失败丢弃、只消费合法新帧”的策略；启停控制帧沿用同一原则：


| 异常场景                     | 当前处理                                          | 安全意义                                     |
| ------------------------------ | --------------------------------------------------- | ---------------------------------------------- |
| 收到非`0xFF/0xAA` 字节       | parser 保持`JETSON_PARSER_WAIT_SOF`，直接忽略     | 串口噪声不会进入业务数据                     |
| `LEN != 0x05`                | `parser_reset()`，丢弃当前帧                      | 防止旧协议或错误长度污染解析                 |
| `FUNC != 0x03`               | `parser_reset()`，丢弃当前帧                      | 旧 K230 的 0x02/0x04/0x06 不会被当作视觉误差 |
| checksum 不匹配              | `parser_reset()`，丢弃当前帧                      | 防止错误`dcx/dcy` 驱动机械臂移动             |
| `EOF != 0xFE`                | 不更新视觉误差并复位 parser                       | 半帧、错帧不会生成新目标                     |
| `AA` 控制帧非法              | 丢弃当前控制帧，不改变`ROBOT_TARGET_ENABLED`      | 错帧不会误启动或误关闭状态机                 |
| Jetson 启动被拒绝            | 保持状态机关闭，拉低 P801，并回传`CC 04 00 DD`    | pose invalid 时不会进入正式定靶              |
| transfer`infoGet()` 失败     | 本周期直接`return`                                | 不使用未知 DMA/DTC 状态                      |
| RX 缓冲写满                  | 先解析尾段，再重新启动`R_SCI_UART_Read()`         | 避免环形接收停住                             |
| UART 打开失败                | 打印错误并`__BKPT(0)`                             | 初始化阶段阻断错误硬件配置                   |
| RX 启动失败                  | 打印错误，不置位新视觉帧                          | 不制造虚假视觉输入                           |
| TX 发送失败或 20 ms 内未完成 | `jetson_send_status_u8()` 返回 `false` 并打印错误 | 状态回传失败不会阻塞控制任务                 |

注意：通信层只负责保证“坏帧不进入状态机”。视觉超时后的安全动作由 4.11 的 `TARGET_ALIGN/TARGET_CONFIRM/TARGET_OUTPUT` 处理。

#### 当前替换结果

- `Middle/K230_cmd.c/.h` 已删除。
- Keil 工程 `Robot_FreeRTOS.uvprojx` 已加入 `jetson_vision.c/.h`，不再加入 `K230_cmd.c/.h`。
- `configuration.xml` 和 `ra_gen/hal_data.*` 的 UART callback 已改为 `robot_jetson_callback`。
- 底层 UART 实例名已改为 `robot_jeston_*` 和 `g_transfer_jeston_rx`。

### 4.11 激光定靶状态机（当前实现）

#### 当前代码位置


| 文件                                | 当前职责                                                                                      |
| ------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `APP/robot_target.c`                | 定靶状态机、视觉误差闭环、小步 AUTO 修正、P801 激光输出安全控制                               |
| `APP/robot_target.h`                | 定靶参数、`target_obs_t`、启用开关和 API                                                      |
| `src/vision_service_thread_entry.c` | 周期组装`target_obs_t` 并驱动 `robot_target_step()`                                           |
| `USER_BSP/bsp_laser.c/.h`           | P801 激光 GPIO 底层，高电平开、低电平关；P000 KEY 发射许可输入                                |
| `Middle/robot_cmd.c`                | 提供`target_enable` / `target_disable` 状态机开关；保留 `laser_on` / `laser_off` 串口调试命令 |

#### 启用开关

```c
volatile bool ROBOT_TARGET_ENABLED = false;
volatile bool ROBOT_TARGET_FIRE_ENABLE = false;
```

- `ROBOT_TARGET_ENABLED=false` 时，`robot_target_step()` 停留在 `TARGET_INIT`，不进入定靶流程；串口 `target_disable` 会立即执行一次 `BSP_Laser_Off()`。
- 串口 `target_enable` 只有在 `ROBOT_STATUS_POSE_VALID` 已置位后才会成功；未先 `soft_reset` 建立有效位姿时会拒绝启用并保持激光关闭。
- 串口 `target_enable` 成功后将 `ROBOT_TARGET_ENABLED` 置为 true，状态机重新接管 P801 激光输出。
- 串口 `target_disable` 将 `ROBOT_TARGET_ENABLED` 置为 false，清除 `ROBOT_TARGET_FIRE_ENABLE` 调试覆盖并立即关闭 P801；随后状态机释放 P801，便于使用 `laser_on` / `laser_off` 做硬件调试。
- Jetson 二进制控制帧 `AA 01 01 BB` / `AA 01 00 BB` 与串口 `target_enable` / `target_disable` 走同一安全入口，不复制第二套启停逻辑。
- Jetson 启动请求同样必须满足 `ROBOT_STATUS_POSE_VALID`；成功回传 `CC 04 01 DD`，失败保持关闭并回传 `CC 04 00 DD`。
- Jetson 关闭请求始终允许执行：清除 `ROBOT_TARGET_ENABLED`、`ROBOT_TARGET_FIRE_ENABLE` 并立即关闭 P801，回传 `CC 04 00 DD`；它不是机械臂急停，也不取消已经进入队列或正在执行的 AUTO。
- P000 板载 KEY 已接入 `target_obs_t.fire_button`，按下为低电平；未按住 KEY 时，即使视觉对准完成，也不会进入正式激光输出。
- `ROBOT_TARGET_FIRE_ENABLE=false` 仍保留为调试/测试覆盖的默认安全状态，不作为比赛正式发射输入。

#### 周期任务运行

`vision_service_thread_entry.c` 当前运行流程：

```c
jetson_vision_init();
robot_target_init();

while (1) {
    target_obs_t obs = {0};
    obs.now_ms = HAL_GetTick();

    jetson_vision_process();
    bool target_enable = false;
    if (jetson_get_target_control(&target_enable)) {
        bool enabled = false;
        if (target_enable) {
            enabled = robot_target_enable_request();
        } else {
            robot_target_disable_request();
        }
        jetson_send_status_u8(RA6_TO_JETSON_TARGET_CTRL, enabled ? 1u : 0u);
    }
    obs.has_vision = jetson_get_vision_error(&obs.dcx, &obs.dcy);
    obs.fire_button = BSP_Laser_FireKey_IsPressed();

    robot_target_step(&obs);
    vTaskDelay(pdMS_TO_TICKS(10));
}
```

也就是说，视觉解析和状态机都是 10 ms 调度；真正的对准运动限频在 `TARGET_ALIGN_PERIOD_MS=250` 内部完成。

#### 状态机运行

当前状态枚举：

```text
TARGET_INIT
  -> TARGET_PRE_POSITION
  -> TARGET_WAIT_DETECT
  -> TARGET_ALIGN
  -> TARGET_CONFIRM
  -> TARGET_OUTPUT
  -> TARGET_DONE / TARGET_RECOVER
```


| 状态                  | 当前实现                                                                                                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TARGET_INIT`         | 关激光，目标设为预定位`TARGET_PRE_X/Y/Z`，清零对准/确认计数，调用 `robot_send_auto_event()` 移动到预定位；发送成功后进入 `TARGET_PRE_POSITION`                                                                           |
| `TARGET_PRE_POSITION` | 持续关激光；当`g_robot.cur_pos` 距目标 X/Y/Z 均不超过 1 mm，回传 `RA6_TO_JETSON_READY=1`，进入 `TARGET_WAIT_DETECT`                                                                                                     |
| `TARGET_WAIT_DETECT`  | 持续关激光；等待`vision_fresh()` 成立，即收到 500 ms 内的新视觉误差帧，进入 `TARGET_ALIGN`                                                                                                                              |
| `TARGET_ALIGN`        | 持续关激光；视觉超时则清零对准/确认计数并进入`TARGET_RECOVER`；误差在阈值内时只在新帧到达时累计稳定帧数；连续 3 帧达标后回传 `RA6_TO_JETSON_ALIGN_DONE=1`，进入 `TARGET_CONFIRM`；未达标时在 AUTO 非 busy 时按粗/精步长发送小步修正 |
| `TARGET_CONFIRM`      | 持续关激光；视觉超时进入`TARGET_RECOVER`；误差丢失则清零对准/确认计数并退回 `TARGET_ALIGN`；只有连续 `TARGET_CONFIRM_STABLE_COUNT` 个新视觉帧达标且 `fire_allowed=true` 时才回传 `RA6_TO_JETSON_OUTPUT=1` 并进入 `TARGET_OUTPUT` |
| `TARGET_OUTPUT`       | `fire_allowed` 仍为 true 且视觉未超时时调用 `BSP_Laser_On()`；许可撤销或视觉超时则先 `BSP_Laser_Off()`，回传 `RA6_TO_JETSON_OUTPUT=0`，进入 `TARGET_DONE`                                                               |
| `TARGET_DONE`         | 关激光；等待`fire_allowed=false` 后清零对准/确认计数并回到 `TARGET_WAIT_DETECT`                                                                                                                                          |
| `TARGET_RECOVER`      | 关激光；等待 `TARGET_ALIGN_PERIOD_MS`（当前 250 ms）后清零对准/确认计数并回到`TARGET_WAIT_DETECT`                                                                                                                       |

全局安全入口：每次 `robot_target_step()` 开头都会检查 `obs->estop_active`、`obs->limit_triggered` 和所有关节的 `ROBOT_STATUS_LIMIT_HAPPENED`。任一触发时立即关激光，回传 `RA6_TO_JETSON_ERROR=1`，进入 `TARGET_RECOVER`。

#### 视觉精瞄实现

当前参数定义在 `robot_target.h`：


| 参数                        |   当前值 | 说明                            |
| ----------------------------- | ---------: | --------------------------------- |
| `TARGET_PRE_X`              |   `0.0f` | 预定位 X                        |
| `TARGET_PRE_Y`              | `-60.0f` | 预定位 Y                        |
| `TARGET_PRE_Z`              |  `-1.5f` | 预定位 Z                        |
| `TARGET_KX_MM_PER_PX`       |  `0.38f` | 像素 X 误差到机械臂 X 修正      |
| `TARGET_KY_MM_PER_PX`       |  `0.35f` | 像素 Y 误差到机械臂 Z 修正      |
| `TARGET_MAX_STEP_MM`        |   `2.5f` | 单次 X/Z 修正限幅（粗定位步长） |
| `TARGET_ALIGN_TOL_PX`       |   `5.0f` | 对准阈值                        |
| `TARGET_ALIGN_STABLE_COUNT` |      `3` | 连续 3 个新视觉帧达标才确认对准 |
| `TARGET_ALIGN_PERIOD_MS`    |    `250` | 两次 AUTO 小步修正的最小间隔    |
| `TARGET_VISION_VALID_MS`    |    `500` | 视觉帧有效期                    |

以上为当前 `robot_target.h` 实际值，此前文档记录的 `-50.0f`/`-0.38f`/`1.5f`/`30.0f`/`200` 已过期。下方 4.12 节的两段式改造将在此基础上新增 `TARGET_ALIGN_TOL_PX_COARSE`、`TARGET_MAX_STEP_MM_FINE` 等参数，不改动本节现有命名。

`TARGET_ALIGN` 的实际修正方式：

```c
float dx = clamp(dcx * TARGET_KX_MM_PER_PX, -TARGET_MAX_STEP_MM, TARGET_MAX_STEP_MM);
float dz = clamp(dcy * TARGET_KY_MM_PER_PX, -TARGET_MAX_STEP_MM, TARGET_MAX_STEP_MM);

target = g_robot.cur_pos;
target.x += dx;
target.z += dz;
robot_send_auto_event(&target);
```

注意这里不是无限累加旧目标，而是以当前 `g_robot.cur_pos` 为基准做小步修正，降低旧目标残留导致的过冲风险。

#### 激光输出安全链路

正式链路只在 `TARGET_OUTPUT` 中调用 `BSP_Laser_On()`，其它状态持续调用或确保 `BSP_Laser_Off()`。退出、超时、限位、急停、未许可时都会先关激光。

`laser_on` / `laser_off` 串口命令仍保留在 `robot_cmd.c`，仅用于 P801 调试。调试时应先发送 `target_disable`，否则状态机在启用后会按安全链路重新接管 P801。它们不会改变 `ROBOT_TARGET_ENABLED`、`ROBOT_TARGET_FIRE_ENABLE`、P000 KEY 读取结果或状态机状态，因此不属于正式定靶安全输出路径。

#### 异常处理与安全保障

当前定靶链路采用“默认关闭、显式启用、持续校验、异常先关断”的策略：


| 保障点               | 当前代码行为                                                                                                      | 作用                                          |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 上电默认低电平       | `BSP_Laser_Init()` 将 P801 配置为输出低电平，并立即调用 `BSP_Laser_Off()`；同时将 P000 配置为输入上拉             | 上电后激光默认关闭，KEY 未按下时许可为 false  |
| 状态机默认禁用       | `ROBOT_TARGET_ENABLED=false`                                                                                      | 未显式启用时不会进入定靶动作                  |
| 串口启用状态机       | `target_enable` 先检查 `ROBOT_STATUS_POSE_VALID`，通过后将 `ROBOT_TARGET_ENABLED=true` 并调用 `BSP_Laser_Off()`   | 未复位时拒绝进入正式定靶                      |
| 串口关闭状态机       | `target_disable` 将 `ROBOT_TARGET_ENABLED=false`、`ROBOT_TARGET_FIRE_ENABLE=false`，并立即调用 `BSP_Laser_Off()`  | 退出正式定靶、清除调试覆盖并关闭 P801         |
| Jetson 启停帧        | `AA 01 01 BB` / `AA 01 00 BB` 复用串口启停的同一安全入口，并用 `CC 04 01/00 DD` 回传结果                          | Jetson 可启动/关闭定靶，但不能绕过 pose 校验  |
| 发射许可默认禁止     | `BSP_Laser_FireKey_IsPressed()` 读取 P000，默认未按下为 false                                                     | 对准完成也不会自动开激光                      |
| 软件调试覆盖默认关闭 | `ROBOT_TARGET_FIRE_ENABLE=false`                                                                                  | 保留测试入口，但比赛正式输入使用 P000 KEY     |
| 禁用时释放调试       | `robot_target_step()` 检测到 `!ROBOT_TARGET_ENABLED` 后只回到 `TARGET_INIT`，不周期性抢占 P801                    | `target_disable` 后可用 `laser_on/off` 调试   |
| 非输出状态关断       | `TARGET_INIT/PRE_POSITION/WAIT_DETECT/ALIGN/CONFIRM/DONE/RECOVER` 均会关激光                                      | 激光只可能在`TARGET_OUTPUT` 内打开            |
| 限位/急停入口        | 每周期检查`obs->estop_active`、`obs->limit_triggered` 和所有关节 `ROBOT_STATUS_LIMIT_HAPPENED`                    | 任一触发立即关激光并进入恢复                  |
| 错误上报             | 限位/急停类异常回传`RA6_TO_JETSON_ERROR=1`                                                                        | Jetson 可停止发送或提示异常                   |
| 视觉超时保护         | `TARGET_ALIGN`、`TARGET_CONFIRM`、`TARGET_OUTPUT` 都检查 `vision_fresh(now)`，500 ms 无新帧即退出当前动作         | Jetson 掉线或视觉卡死时不会继续输出           |
| 小步修正限幅         | X/Z 粗定位单步限制在 `±2.5 mm`，精定位单步限制在 `±0.8 mm`，且两次修正间隔至少 250 ms                              | 降低视觉误差突变造成的大动作风险              |
| 对准稳定判定         | 只有连续 3 个新视觉帧都在 `TARGET_ALIGN_TOL_PX` 内才进入 `TARGET_CONFIRM`                                        | 防止单帧抖动误判为对准                        |
| 输出二次确认         | `TARGET_CONFIRM` 内还需连续 `TARGET_CONFIRM_STABLE_COUNT` 个新视觉帧达标；对准/确认计数在状态切换和异常退回时一起清零 | 防止上一靶确认计数泄漏到下一靶                |
| 输出保持条件         | `TARGET_OUTPUT` 中必须持续满足 `fire_allowed=true` 且视觉未超时；正式链路中 `fire_allowed` 来自 P000 KEY 按住状态 | 松开 KEY 或视觉失效立即关断                   |
| 并发禁用保险         | `TARGET_OUTPUT` 在短临界区内再次检查 `ROBOT_TARGET_ENABLED`，只有仍启用时才调用 `BSP_Laser_On()`                  | 串口禁用和状态机同周期交错时不会重新点亮 P801 |
| 输出切断回传         | 从`TARGET_OUTPUT` 因许可撤销或视觉超时退出时回传 `RA6_TO_JETSON_OUTPUT=0`                                         | 上位机可同步显示输出已关闭                    |
| 调试命令隔离         | `laser_on/off` 只直接控制 P801，不改变 `ROBOT_TARGET_ENABLED`、P000 KEY 读取结果或状态机状态                      | 保留硬件调试能力，但不混入正式安全链路        |

当前限制必须明确记录：

- P000 KEY 已作为正式发射许可输入，默认按下为低电平；若上板实测相反，只需把 `LASER_FIRE_KEY_ACTIVE_LEVEL` 改为 `BSP_IO_LEVEL_HIGH`。
- `ROBOT_TARGET_FIRE_ENABLE` 仅作为调试/测试覆盖保留，不作为比赛正式发射输入。
- `target_disable` 不等于机械臂急停，也不取消已经进入 robot 控制队列或正在执行的 AUTO 运动；它只关闭定靶状态机和 P801 激光输出。
- Jetson 关闭控制帧 `AA 01 00 BB` 语义与 `target_disable` 一致，同样不是机械臂急停。
- `obs->estop_active` 和 `obs->limit_triggered` 已预留在 `target_obs_t`，当前周期任务尚未接入独立急停 GPIO；代码已检查关节限位状态 `ROBOT_STATUS_LIMIT_HAPPENED`。
- 本文档记录的是代码静态实现，P801 电平、P000 KEY 许可、视觉超时关断、限位触发关断仍需上板验证。

#### 当前替换结果

- `APP/robot-take.c/.h` 已删除。
- Keil 工程 `Robot_FreeRTOS.uvprojx` 已加入 `robot_target.c/.h`，不再加入 `robot-take.c/.h`。
- 采摘相关的 Z 扫查、Y 逼近、夹爪抓取、摘取、放置、草莓数量和可采摘判断逻辑已移除。
- 当前实现已完成代码静态接入，但尚未在当前环境完成 Keil 编译和上板验证。

### 4.12 末端精度与分辨率提升（计划，未实施）

改进目标：进一步提升激光定靶最终落点精度和可重复性，同时不牺牲预定位等演示动作的速度感。以下小节均为计划，尚未修改 `robot_target.c/.h` 或 `robot.c` 代码。

对当前代码核实后的可实施结论：4.12 不应通过直接降低全局 `SCURVE_VMAX/AMAX` 来实现，否则演示 `auto`、预定位和精瞄都会一起变慢。第一阶段只做三件事：两段式步长、`ROBOT_AUTO_EVENT` busy 防排队、`TARGET_CONFIRM` 二次确认。降低 `Amax`、可变 `vmax/amax`、可变终点稳定周期属于第二阶段实验项，只有在日志或实测证明小步运动仍有冲击/过冲后再做。

实施顺序固定为：

1. 先做两段式步长，只影响 `TARGET_ALIGN` 的 `dx/dz` 限幅。
2. 再做 `ROBOT_AUTO_EVENT` busy 原语，确保 `TARGET_ALIGN` 不会在上一条 AUTO 未完成时继续塞新 AUTO。
3. 再做 `TARGET_CONFIRM` 二次视觉确认。
4. 最后根据实测决定是否增加精瞄专用 `Amax/settle` 参数。

这个顺序可以逐步验证，每一步都有明确回退点；不先大改插补框架，也不改全局演示动作参数。

#### 4.12.1 两段式接近：粗定位 + 精定位切换步长

现状：`TARGET_ALIGN` 无论视觉误差大小，单次修正步长都固定为 `TARGET_MAX_STEP_MM = 2.5mm`（见 4.11 表），只有当 `|dcx|`、`|dcy|` 都进入 `TARGET_ALIGN_TOL_PX = 5.0px` 才算完成对准。误差较大时靠近慢，误差较小时步长又偏大，容易在目标附近来回修正。

计划新增：


| 新增参数                     |  建议值 | 作用                                      |
| ------------------------------ | --------: | ------------------------------------------- |
| `TARGET_ALIGN_TOL_PX_COARSE` | `15.0f` | 粗/精步长切换阈值，误差大于该值时用粗步长 |
| `TARGET_MAX_STEP_MM_FINE`    |  `0.8f` | 精定位步长，误差进入切换阈值内后使用      |

`TARGET_MAX_STEP_MM`（2.5mm）保留为粗定位步长，`TARGET_ALIGN_TOL_PX`（5.0px）保留为进入 `TARGET_CONFIRM` 的最终对准判据，均不改动。

`TARGET_ALIGN` 内修正逻辑计划调整为：

```c
float err_px = fmaxf(fabsf((float)s_target.dcx), fabsf((float)s_target.dcy));
float step_limit = (err_px <= TARGET_ALIGN_TOL_PX_COARSE) ? TARGET_MAX_STEP_MM_FINE
                                                            : TARGET_MAX_STEP_MM;
float dx = clampf_local(dcx * TARGET_KX_MM_PER_PX, -step_limit, step_limit);
float dz = clampf_local(dcy * TARGET_KY_MM_PER_PX, -step_limit, step_limit);
```

预期效果：远距离仍用 2.5mm 快速逼近，进入 15px 范围后自动切到 0.8mm 小步修正，减少最后阶段的过冲和来回震荡。

#### 4.12.2 `ROBOT_AUTO_EVENT` busy 防排队

现状风险：`TARGET_ALIGN_PERIOD_MS=250ms`，但一个 2.5mm 小步未必能在 250ms 内完成。当前状态机只按时间间隔发 AUTO，不知道上一条 AUTO 是否执行完成，因此存在“视觉帧持续到来 -> 反复发送 AUTO -> 机械臂队列滞后”的风险。

这个问题不需要在 `robot_target.c` 里单独新增 `align_move_pending/align_move_start_ms/TARGET_ALIGN_MOVE_TIMEOUT_MS`，但也不能把它误扩展成 4.9 的“关节位置命令到位”信号。4.12 只需要知道笛卡尔 `ROBOT_AUTO_EVENT` 是否已入队或还在执行，因此在 `robot.c` 内建立一个范围明确的 AUTO busy 原语：


| 新增项                                  | 作用                          |
| ----------------------------------------- | ------------------------------- |
| `ROBOT_STATUS_AUTO_BUSY` 或等价全局标志 | `ROBOT_AUTO_EVENT` 已入队或正在执行 |
| `robot_is_auto_busy()`                  | 供 `TARGET_ALIGN` 和后续直接发 `ROBOT_AUTO_EVENT` 的调用方查询 |

执行规则：

- `robot_send_auto_event()` 成功入队前置位 busy，避免事件尚未被控制任务取走时重复入队。
- `robot_auto_move_interpolation()` 入口再次置位 busy，保持执行期状态明确。
- `robot_pid_run()` 正常返回、IK 失败、路径生成失败等所有退出路径都清 busy。
- `TARGET_ALIGN` 发小步 AUTO 前先查 `robot_is_auto_busy()`；busy 时只更新最新视觉误差缓存，不发送新 AUTO。
- `TARGET_ALIGN` 不再自己判断“上一小步是否到位”，也不维护独立 timeout。

这个标志只回答“上一条笛卡尔 AUTO 是否已入队或尚未结束”，不回答关节相对/绝对位置命令是否到位。4.9 涉及 `ROBOT_JOINT_REL_ROTATE`、`ROBOT_JOINT_ABS_ROTATE` 等位置命令，后续应单独设计关节到位确认来源，不能复用 `ROBOT_STATUS_AUTO_BUSY`。

#### 4.12.3 精瞄专用低速运动档（第二阶段实验项）

现状核实：`SCURVE_VMAX=180mm/s`、`SCURVE_AMAX=300mm/s²` 是 `robot.c` 中的文件级常量，`robot_path_interpolation_scurve()` 直接使用，所有 AUTO 事件（演示 `auto` 命令和 `robot_target.c` 内部的对准修正）共用同一组参数，改全局参数会影响演示动作。

关键计算（先算清楚是否真的需要降 Vmax）：两段式（短路径）峰值速度公式为 `Vpeak = sqrt(S * Amax * π/2)`。`TARGET_ALIGN` 单步已被 `TARGET_MAX_STEP_MM=2.5mm` 限幅，代入当前 `Amax=300mm/s²`：

```text
Vpeak = sqrt(2.5 * 300 * 1.5708) ≈ 34.3 mm/s
```

也就是说，对准阶段的小步修正因为距离本身很短，峰值速度早已远低于全局 `Vmax=180mm/s`，Vmax 目前不是主要限制因素。降低 `Amax` 可能改善冲击感，但需要改 `robot_send_auto_event()`、`robot_auto_move_interpolation()`、`robot_path_interpolation_scurve()`、`robot_pid_run()` 等多处函数签名，还要重新计算 `omega/t_accel/s_accel`，改动成本明显高于 4.12.1/4.12.2/4.12.4。

因此本项暂不作为第一阶段必做项。第一阶段先用“两段式步长 + `ROBOT_AUTO_EVENT` busy + 二次确认”验证末端稳定性；只有在日志或实测证明小步仍有明显冲击、过冲或无法收敛时，再引入精瞄专用 `Amax/settle`。

如果后续确实要做，约束如下：

- 不改全局 `SCURVE_VMAX/AMAX`。
- `robot_send_auto_event()` 保持兼容，串口 `auto` 和 `TARGET_INIT` 预定位仍走默认参数。
- 只允许 `TARGET_ALIGN` 调用扩展入口。
- 传入 `vmax/amax` 后，`robot_path_interpolation_scurve()` 必须在函数内部重新计算 `omega`、`t_accel`、`s_accel`。

#### 4.12.4 终点稳定延长 + 输出前二次确认

改造前核实：末端稳定段是 `robot_pid_run()` 末尾的固定循环 `for (k=0;k<ROBOT_PID_SETTLE_PERIODS;k++)`，`ROBOT_PID_SETTLE_PERIODS=10`，按 10ms 控制周期计算约 100ms，前馈清零只留 P 项收敛，这是全局值，所有 AUTO 运动共用。旧逻辑中 `TARGET_CONFIRM` 只要 `align_in_tolerance()` 为真且 `fire_allowed` 为真，当次循环就可能直接进入 `TARGET_OUTPUT`，中间没有额外的稳定等待或二次校验。

第一阶段已改动两处：

##### a) 精定位终点稳定延长（第二阶段实验项）

状态机在发出一条修正指令时，无法提前知道“这是否是最后一次修正”。最终是否落进 `TARGET_ALIGN_TOL_PX=5px`，必须等这步执行完成并收到下一帧视觉数据后才能判断。因此不能写成“最后一次修正步使用 `TARGET_SETTLE_PERIODS_FINE`”。

如果后续引入可变 `settle_periods`，正确条件应与 4.12.1 的粗/精切换一致：只要 `err_px <= TARGET_ALIGN_TOL_PX_COARSE`，该次精定位小步就使用 `TARGET_SETTLE_PERIODS_FINE`；粗定位区间继续使用全局 `ROBOT_PID_SETTLE_PERIODS`。是否真的需要该项，必须在第一阶段完成后用日志和实测落点判断，不作为立即实施内容。

##### b) 进入 `TARGET_CONFIRM` 后二次确认，而不是当次循环立即放行

新增 `s_target.confirm_stable_count` 计数器（用法与 `stable_count` 一致）和参数 `TARGET_CONFIRM_STABLE_COUNT=4`，对应约 3~5 个新视觉帧。`TARGET_CONFIRM` 当前实现为：

```c
case TARGET_CONFIRM:
    force_laser_off();
    if (!vision_fresh(now)) { enter_state(TARGET_RECOVER, now); break; }
    if (!align_in_tolerance()) {
        reset_alignment_counts();
        enter_state(TARGET_ALIGN, now);
        break;
    }
    if (new_vision && (s_target.confirm_stable_count < TARGET_CONFIRM_STABLE_COUNT)) {
        s_target.confirm_stable_count++;
    }
    if ((s_target.confirm_stable_count >= TARGET_CONFIRM_STABLE_COUNT) && fire_allowed) {
        jetson_send_status_u8(RA6_TO_JETSON_OUTPUT, 1u);
        enter_state(TARGET_OUTPUT, now);
    }
    break;
```

即对准判定（进 `TARGET_ALIGN`→`TARGET_CONFIRM`）用 `TARGET_ALIGN_STABLE_COUNT=3` 帧，输出判定（进 `TARGET_CONFIRM`→`TARGET_OUTPUT`）再单独用 `TARGET_CONFIRM_STABLE_COUNT` 帧二次确认，两段各自计数，互不复用，且都要求“新视觉帧”而非同一帧重复计数，避免视觉丢帧或卡帧时被误判为稳定。代码中统一使用 `reset_alignment_counts()` 同时清零 `stable_count` 和 `confirm_stable_count`；状态切换、视觉超时、误差丢失、`TARGET_DONE` 回到等待、`TARGET_RECOVER` 回到等待时都必须两个计数一起清，避免上一靶的确认计数泄漏到下一靶。

Jetson 侧约束：收到 `CC 02 01 DD`（ALIGN_DONE）后不能停止发送视觉误差帧，必须继续按 200~250ms 周期发送新帧，直到 RA6M5 进入 `TARGET_OUTPUT` 或回到 `TARGET_ALIGN/RECOVER`。否则二次确认拿不到新视觉帧，会停在 `TARGET_CONFIRM` 或因 500ms 视觉超时进入 `TARGET_RECOVER`。

对比赛展示效果：从"对准即出光"变为"对准后再持续验证 3~5 帧才出光"，在数据上更容易解释为"二次确认机制降低误触发概率"。

#### 4.12.5 第一阶段新增参数汇总


| 新增参数                            |  建议值 | 作用                                        | 涉及文件            |
| ------------------------------------- | --------: | --------------------------------------------- | --------------------- |
| `TARGET_ALIGN_TOL_PX_COARSE`        | `15.0f` | 粗/精步长切换阈值                           | `robot_target.h/.c` |
| `TARGET_MAX_STEP_MM_FINE`           |  `0.8f` | 精定位步长                                  | `robot_target.h/.c` |
| `ROBOT_STATUS_AUTO_BUSY` 或等价标志 |  `1bit` | `ROBOT_AUTO_EVENT` 已入队或执行中，只服务笛卡尔 AUTO 防排队 | `robot.h/.c`        |
| `TARGET_CONFIRM_STABLE_COUNT`       |     `4` | `TARGET_CONFIRM` 二次确认所需连续新视觉帧数 | `robot_target.h/.c` |

`SCURVE_VMAX/AMAX`（180/300，演示动作）、`TARGET_MAX_STEP_MM`（2.5mm，粗定位）、`TARGET_ALIGN_TOL_PX`（5.0px，最终对准判据）、`TARGET_ALIGN_STABLE_COUNT`（3，对准判定帧数）、`ROBOT_PID_SETTLE_PERIODS`（10，通用终点固定稳定周期）均保留当前实现。`TARGET_MOVE_VMAX_MM_S`、`TARGET_MOVE_AMAX_MM_S2`、`TARGET_SETTLE_PERIODS_FINE` 暂不进入第一阶段参数表，避免在没有实测依据前扩大改动面。

#### 4.12.6 验证重点

1. 串口 `auto` 和 `TARGET_INIT` 预定位耗时不变，确认默认 `robot_send_auto_event()` 仍走全局 `SCURVE_VMAX/AMAX`。
2. `TARGET_ALIGN` 中连续收到视觉误差时，`ROBOT_STATUS_AUTO_BUSY` 为真时不得发送新 AUTO；日志中不能出现旧目标排队执行。
3. 误差进入 `TARGET_ALIGN_TOL_PX_COARSE=15px` 后，单步限幅从 2.5mm 切换到 0.8mm。
4. `TARGET_CONFIRM` 期间即使 P000 已按下，也必须等到 `TARGET_CONFIRM_STABLE_COUNT` 个新视觉帧达标后才回传 `CC 03 01 DD` 并打开 P801。
5. Jetson 在 ALIGN_DONE 后继续发帧；停止发帧时 RA6M5 应因 `TARGET_VISION_VALID_MS=500ms` 超时回到安全状态，P801 保持关闭。

除第四章主线改进外，当前代码还包含以下修复：

- 路径和 IK 结果改为静态缓冲区，避免长路径运行时 `malloc` 失败。
- 增加 `vApplicationMallocFailedHook()` 和 `vApplicationStackOverflowHook()`。
- soft reset 使用自适应 settle timeout、HOME 最终校验和 3 轮 refine。
- `robot_angle_map()` 边界比较使用包含端点的容差夹紧。
- J2/J3/J5 软件限位相对 HOME 留出 2 度余量。
- CAN stop 命令改为批量发送、统一等待。
- 批量反馈和 stop 等待完成或超时后清理 RX 信号量。
- AUTO 运行前检查位姿有效性，反馈退化状态可被明确记录。
- `ave_error` 改为按实际控制采样次数统计。

---

## 6. 关键参数速查


| 参数                    |                                 当前值 | 说明                                         |
| ------------------------- | ---------------------------------------: | ---------------------------------------------- |
| 控制周期                |                                  10 ms | 100 Hz                                       |
| AUTO S 曲线路径节点间隔 |                                  10 ms | 每点对应一个控制周期，前馈按 `FF_GAIN` 缩放 |
| 时间函数路径节点间隔    |                                  10 ms | 由时间函数直接生成，不经过 S 曲线规划器      |
| remote 目标更新时间     |                                  50 ms | remote 前馈时间基准                          |
| S 曲线最大速度          |                               180 mm/s | `SCURVE_VMAX`                                |
| S 曲线最大加速度        |                             300 mm/s² | `SCURVE_AMAX`                                |
| 线性插值分辨率          |                                   1 mm | 已由 AUTO S 曲线替代，宏保留备用             |
| 最大路径点数            |                                    600 | 达到上限后末点强制替换为目标，需验证跳变风险 |
| Kp                      | `{0.65, 4.00, 3.00, 2.00, 2.30, 10.0}` | 各关节独立，单位`1/s`                        |
| 前馈系数 FF_GAIN        | `{1.0, 0.70, 0.55, 1.0, 0.60, 1.0}`    | 各关节独立前馈缩放，见 4.9.6                 |
| Ki / Kd                 |                                     无 | 当前控制器为前馈 + P                         |
| 控制输出限幅            |                         正负 200 度/秒 | 进入电机 RPM 换算前限幅                      |
| 终点稳定周期            |                                     10 | 前馈清零，按 10 ms 周期合计约 100 ms         |
| 批量角度反馈等待        |                                   6 ms | `BSP_CAN_WaitAllMotors()` 调用值             |
| 通用 CAN 超时宏         |                                  15 ms | `ROBOT_CAN_TIMEOUT`                          |
| 关节边界容差            |                                 0.1 度 | `ROBOT_JOINT_ANGLE_ERROR_RANGE`              |
| soft reset 边界扩展容差 |                                 1.0 度 | 仅 soft reset 映射使用                       |
| soft reset refine pass  |                                      3 | 每关节每 pass 最多 3 轮                      |
| soft reset refine 容差  |                                 1.0 度 | HOME 最终校验容差                            |

---

## 7. 验证状态与建议顺序

### 7.1 当前验证状态

- 本文记录的是当前代码实现状态，不等同于已完成硬件验收。
- 当前 Codex 环境未检测到 `UV4.exe`、ARMCC 或 ARMCLANG，因此未完成 `Robot_FreeRTOS.uvprojx / Target_1` 编译验证。
- 前馈 + P、100 Hz、CAN 并行化、IK 分支锁定、S 曲线和 soft reset 仍需在真实机械臂上完成回归验证。

### 7.2 上板验证建议

1. 执行 `soft_reset`，确认最终 HOME 校验通过且无需重复发送。
2. 运行 `read_all`，检查 J1~J5 批量反馈、耗时和 missing mask。
3. 运行 `auto 15 0 0`，记录运动平滑性、各关节 `ave_error` 和 CAN 超时日志。
4. 运行 `auto 0 -70 0`、`auto 0 -140 0` 和 `auto 0 0 100`，覆盖短路径、长路径和 Z 轴 S 曲线。
5. 修复并验证近零位移的单路径点边界，确认不会把关节目标误置为零。
6. 重复运行正负方向小位移，确认 IK 分支不跳变。
7. 在接近腕部奇异区前停止扩展测试；4.7 完成前不把奇异区运动视为已解决。
8. 上电后不启用 `ROBOT_TARGET_ENABLED`，测量 P801 应保持低电平。
9. 未执行 `soft_reset` 或 pose invalid 时发送 `target_enable`，确认命令被拒绝并提示先复位。
10. 发送 `target_disable` 后执行 `laser_on` / `laser_off`，确认串口调试能直接控制 P801。
11. `soft_reset` 成功后发送 `target_enable`，Jetson 连续发送合法视觉帧，确认 `TARGET_ALIGN` 只按 `±2.5 mm` 限幅小步修正。
12. 停止发送视觉帧超过 500 ms，确认状态机退出对准/输出并拉低 P801。
13. 触发任一关节限位，确认状态机进入 `TARGET_RECOVER`、P801 拉低，并回传 `RA6_TO_JETSON_ERROR=1`。
14. P000 KEY 未按下时，即使连续 3 帧对准，也不得进入激光输出；按住 P000 后才允许 `TARGET_OUTPUT`。
15. 在 `TARGET_OUTPUT` 中松开 P000，确认 P801 立即拉低并回传 `RA6_TO_JETSON_OUTPUT=0`。
16. 在 `TARGET_OUTPUT` 中发送 `target_disable`，确认 P801 立即拉低且不会被状态机重新点亮。
17. pose invalid 时发送 `AA 01 01 BB`，应回传 `CC 04 00 DD`，P801 保持低电平。
18. `soft_reset` 成功后发送 `AA 01 01 BB`，应回传 `CC 04 01 DD` 并进入预定位；任意时刻发送 `AA 01 00 BB`，应立即关闭 P801 并回传 `CC 04 00 DD`。
19. 回归现有视觉状态回传，确认 `READY`、`ALIGN_DONE`、`OUTPUT_ON/OFF` 不受 `FUNC=0x04` 启停回传影响。
20.（4.12 实施后）视觉误差从大误差逐步收敛时，用示波器/日志确认误差降到 `TARGET_ALIGN_TOL_PX_COARSE=15px` 内后单步修正量从 2.5mm 降到 0.8mm。
21.（4.12 实施后）连续发送 200~250ms 周期视觉误差时，确认 `ROBOT_STATUS_AUTO_BUSY` 为真期间 `TARGET_ALIGN` 不会继续发送新的小步 AUTO。
22.（4.12 实施后）确认 `TARGET_CONFIRM` 在达到 `TARGET_CONFIRM_STABLE_COUNT` 帧之前，即使 `fire_allowed=true`，也不会进入 `TARGET_OUTPUT`；期间任一帧超出 `TARGET_ALIGN_TOL_PX` 需退回 `TARGET_ALIGN` 并清零两个计数器。
23.（4.12 实施后）人为在精定位阶段短暂遮挡镜头制造误差抖动，确认 `confirm_stable_count` 归零重新计数，不会因单帧异常直接放行输出。

### 7.3 后续实施顺序

```text
先完成硬件回归
    -> 4.7 奇异点三次阻尼
    -> 4.9 通用位置模式到位确认
    -> 4.12 末端精度与分辨率提升（两段式步长 -> `ROBOT_AUTO_EVENT` busy -> 二次确认；低 Amax/延长 settle 先不做）
    -> 4.1 工程彻底裁剪（移除底盘/DHT11 等废弃模块）
```

4.12 第一阶段不依赖 4.9；`ROBOT_STATUS_AUTO_BUSY` 只服务笛卡尔 `ROBOT_AUTO_EVENT` 防排队，不能作为 4.9 的关节位置命令到位信号。若 4.7/4.9 与 4.12 同期推进，应分开验证，避免把奇异点权重、关节到位确认和视觉精瞄改动混在一起排查。
