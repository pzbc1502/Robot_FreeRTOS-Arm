使用reisaRA6M5系列的主控板，芯片型号为R7FA6M5BF2CBG



USER_BSP作为底层驱动层
Middle作为中间层
APP层作为应用层，其中最主要的robort.c文件在这里，用于控制机械臂以及实现二次开发使用，robort_task.c作为本机械臂采摘项目的状态机执行文件


1、机械臂整臂控制改为仅 J1~J5，J6 不再参与整臂 PID 控制。
2、auto 与 time_func 轨迹中新增 J6 角度锁定，运动过程中保持固定，避免夹爪乱动。
3、逆解最优解权重中将 J6 权重设为 0，避免选解受 J6 干扰。
4、remote 路径移除对 J6 的速度驱动，J6 仅由夹爪模块命令控制。
5、soft_reset 已按安全顺序仅处理 J1~J5（跳过 J6），与硬复位策略一致，降低碰撞风险。
