#### 项目一键演示流程审查

**目标**：把人脸三视图拍摄、Jetson 选择疑似黑痣图像、机械臂移动到对应预开始点、视觉伺服定靶和 P801 输出串成一个完整比赛演示流程。

**正式演示原则**：

- 正式比赛演示应由 Jetson 协议主控，不以 PC 上位机手动发送拍摄配方作为最终链路。
- 正式通信统一使用 `A5 5A + CRC16` 新协议，旧 `AA/FF/CC` 只作为调试兼容。
- Jetson 只下发点位序号、视觉误差和安全距离，不直接下发任意机械臂坐标。
- RA6M5 只执行已验证的左/正/右三组预设点位，降低误发坐标导致自碰撞或越界的风险。
- 安全距离目标是全局保护：普通 `auto`、拍摄点位、定靶状态机都应受安全距离约束。

---

### 1. 当前可以完成的部分

当前一键定靶状态机基本可用，已具备以下链路：

```text
TARGET_CTRL enable
  -> 安全距离有效
  -> PRE_POSITION
  -> READY
  -> VISION_ERROR
  -> ALIGN_DONE
  -> P000 按住
  -> OUTPUT_ON / OUTPUT_OFF
  -> target_disable / 回 HOME
```

当前新协议已经支持：

| 消息 | 当前状态 |
|---|---|
| `HEARTBEAT` | 已支持，用于判断 Jetson 链路在线 |
| `TARGET_CTRL` | 已支持，用于启停定靶状态机 |
| `VISION_ERROR` | 已支持，用于发送 `dcx/dcy` 视觉误差 |
| `SAFE_DISTANCE` | 已支持，但当前只在 `robot_target` 定靶流程中生效 |
| `STATUS/ERROR` | 已支持，用于 READY、ALIGN_DONE、OUTPUT、SAFE_DISTANCE、ERROR 回传 |

---

### 2. 当前三视图拍摄点位配方

当前三视图点位仍是 **PC 上位机文本命令配方**，用于人工调试和拍摄验证；它还不是 Jetson 通过 `CAPTURE_CTRL` 协议驱动 RA6M5 执行的正式点位流程。

```text
左视图：
soft_reset
auto 0 -130 -15
abs_rotate 0 65
abs_rotate 3 330
abs_rotate 4 85
soft_reset

正视图：
soft_reset
auto 0 -130 -15
abs_rotate 0 90
soft_reset

右视图：
soft_reset
auto 0 -130 -15
abs_rotate 0 115
rel_rotate 3 35
abs_rotate 4 80
soft_reset
```

这组三视图点位的作用是让末端相机分别获得人脸左视图、正视图和右视图。后续如果要做成正式项目演示，应将这些配方固化为 RA6M5 端的安全点位，而不是继续依赖 PC 上位机逐条发送文本命令。

---

### 3. 当前不足

1. `CAPTURE_CTRL` 尚未真正闭环  
   `JETSON_MSG_CAPTURE_CTRL = 0x04` 当前只在协议文档和头文件宏中出现，`jetson_vision.c` 没有解析执行 `action + point_id`，RA6M5 也没有拍摄点位状态机。

2. 三视图到位回传尚未实现  
   文档中规划的 `STATUS event=0x10/0x11/0x12` 尚未形成实际代码链路，因此 Jetson QT 目前无法只依靠 RA6M5 回传判断“左/正/右拍摄点位已到达”。

3. Jetson 选择图片后无法直接驱动 RA6M5 到对应预开始点  
   目前 RA6M5 还不能根据 `point_id=1/2/3` 自动进入对应的左/正/右定靶预开始点。

4. 安全距离不是全局保护  
   当前安全距离只在 `robot_target_step()` 中处理。普通 `auto`、三视图拍摄点位、手动调试动作不会因为距离过近自动后退。

5. PC 上位机拍摄配方不等于正式 Jetson 演示链路  
   上位机“拍摄点位”页可以发送文本命令，但这只是调试工具；正式比赛流程应由 Jetson 通过统一协议下发点位序号，并由 RA6M5 执行已验证点位。

---

### 4. 没有意识到的风险

- 如果把 PC 上位机配方当成 Jetson 协议能力，会造成比赛演示链路描述与实际代码不一致。
- 如果 Jetson 直接下发任意坐标，可能因坐标错误导致机械臂进入危险姿态；因此应只允许 `point_id=1/2/3`。
- 如果安全距离只在定靶阶段生效，三视图拍摄时末端仍可能靠近目标区域，这与“全局安全距离保护”的目标不一致。
- 如果心跳中断只关闭激光但不处理当前运动，机械臂可能继续执行已进入队列的 AUTO，需要明确是否中断运动或回 HOME。
- 如果每个拍摄点位末尾都执行 `soft_reset`，拍摄流程会更安全，但演示耗时会变长；后续需要在安全性和展示连贯性之间取舍。

---

### 5. 后续实现方向

#### 5.1 固件新增 `CAPTURE_CTRL` 处理

推荐继续使用统一协议：

```text
A5 5A 01 04 SEQ 02 action point_id CRC16_LO CRC16_HI
```

| action | point_id | 含义 |
|---:|---:|---|
| `0x01` | `1/2/3` | 移动到左/正/右拍摄点位 |
| `0x02` | `0` | 三视图拍摄完成，执行 HOME |
| `0x03` | `1/2/3` | Jetson 选择黑痣所在视图，进入对应定靶预开始点 |

推荐状态回传：

| event | value | 含义 |
|---:|---:|---|
| `0x10` | `1/2/3` | 已到达左/正/右拍摄点位，Jetson 可拍照 |
| `0x11` | `1` | 三视图拍摄完成，已回 HOME |
| `0x12` | `1/2/3` | 已进入 Jetson 选中的定靶预开始点 |

#### 5.2 固化三视图安全点位

RA6M5 端应内置三组已验证点位，不允许 Jetson 直接下发任意坐标。当前 PC 配方可以作为第一版固化来源：

- `point_id=1`：左视图点位。
- `point_id=2`：正视图点位。
- `point_id=3`：右视图点位。

#### 5.3 全局安全距离保护

安全距离应从 `robot_target` 中抽出为全局安全输入：

- Jetson 周期发送 `SAFE_DISTANCE`。
- RA6M5 在普通 `auto`、拍摄点位、定靶状态机中都检查距离。
- 距离低于阈值时关闭 P801，中断或停止当前靠近动作，并执行 Y 方向后退或进入安全恢复。

---

### 6. 后续验证清单

1. Jetson 发送 `CAPTURE_CTRL action=0x01 point_id=1/2/3`，RA6M5 能到对应点位并回传 `event=0x10`。
2. 左/正/右三视图依次到位后，Jetson QT 能保存三张图。
3. Jetson 发送 `action=0x03 point_id=1/2/3` 后，RA6M5 能进入对应定靶预开始点并回传 `event=0x12`。
4. `TARGET_CTRL enable` 后，仍能收到 READY、ALIGN_DONE、OUTPUT_ON/OFF。
5. 普通 `auto`、拍摄点位、定靶过程中发送 `distance<100mm`，P801 保持关闭，机械臂停止靠近并进入安全后退或恢复。
6. 新协议心跳中断超过 600 ms，RA6M5 关闭 P801，退出危险状态并回传错误。
