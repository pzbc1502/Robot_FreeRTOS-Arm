### 0、Jetson 端对接速查

正式项目演示统一使用第 4 章的 `A5 5A + CRC16` 协议；第 1~3 章旧 `FF/AA/CC` 协议只保留给 PC 调试和兼容验证。Jetson 端不需要下发机械臂坐标、关节角或 `auto` 文本命令，只需要发送消息类型、动作号、点位号、视觉误差和安全距离。

串口建议：

| 项目 | 建议值 |
|---|---|
| 波特率 | `115200` |
| 数据位 | `8` |
| 校验 | `None` |
| 停止位 | `1` |
| 字节序 | 多字节字段全部小端 |

Jetson 端最小必做：

1. 串口打开后每 `100~200 ms` 发送 `HEARTBEAT`。
2. 定靶前周期发送 `SAFE_DISTANCE distance_mm valid`，建议 `100~200 ms` 一帧。
3. 发送 `TARGET_CTRL enable=1` 启动定靶状态机。
4. 收到 `STATUS event=0x01 value=1` 后发送 `VISION_ERROR dcx dcy valid=1`。
5. 收到 `STATUS event=0x02 value=1` 后提示用户按住 P000，等待 `OUTPUT_ON/OFF`。
6. 三视图项目演示使用 `CAPTURE_CTRL action + point_id`，Jetson 只下发点位编号。

当前实现状态提示：

- `HEARTBEAT`、`TARGET_CTRL`、`VISION_ERROR`、`SAFE_DISTANCE` 已有代码基础，可用于定靶链路联调。
- `CAPTURE_CTRL action=0x01/0x02/0x03`、`STATUS event=0x10/0x11/0x12` 和 RA6M5 端三视图点位固化已补入固件与上位机模拟器，仍需 Keil 编译和上板联调确认。

#### 1、Jetson -> RA6M5 视觉误差帧（旧协议，调试兼容）

旧协议保留用于串口调试和兼容验证，正式项目演示不再以该帧作为主链路。旧链路的视觉误差帧格式如下：

```text
[0xFF][0x05][0x03][dcx_lo][dcx_hi][dcy_lo][dcy_hi][checksum][0xFE]
```


| 字段     |     值 | 说明                                       |
| ---------- | -------: | -------------------------------------------- |
| SOF      | `0xFF` | 帧起始                                     |
| LEN      | `0x05` | `FUNC(1) + DATA(4)`                        |
| FUNC     | `0x03` | `JETSON_FUNC_VISION_ERROR`，视觉误差帧     |
| DATA     | 4 字节 | `int16_t dcx` + `int16_t dcy`，小端        |
| CHECKSUM | 1 字节 | `LEN + FUNC + DATA...` 的 `uint8_t` 截断和 |
| EOF      | `0xFE` | 帧结束                                     |

解析状态机只接受 `SOF=0xFF`、`LEN=0x05`、`FUNC=0x03`、4 字节 payload、匹配 checksum 和 `EOF=0xFE`。
`dcx/dcy` 含义：

- `dcx`：画面 X 轴像素偏差，正方向由 Jetson 端定义，映射到机械臂 X 修正。
- `dcy`：画面 Y 轴像素偏差，当前控制侧按  映射到机械臂 Z 修正。

#### 2、Jetson -> RA6M5 定靶状态机启停控制帧（旧协议，调试兼容）

旧协议在同一个 Jetson UART 接收通道中兼容 4 字节启停控制帧，不新建第二套通信模块：

```text
[0xAA][FUNC][VALUE][0xBB]
```


| 字段  | 值            | 说明                     |
| ------- | --------------- | -------------------------- |
| SOF   | `0xAA`        | 启停控制帧头             |
| FUNC  | `0x01`        | 定靶状态机控制           |
| VALUE | `0x01`/`0x00` | `0x01` 启动，`0x00` 关闭 |
| EOF   | `0xBB`        | 启停控制帧尾             |

示例：

```text
AA 01 01 BB   // Jetson 请求启动 target 状态机
AA 01 00 BB   // Jetson 请求关闭 target 状态机
```

解析策略：

- `0xFF ... 0xFE` 仍按现有视觉误差帧解析，保持 `FF 05 03 ... checksum FE` 不变。
- `0xAA 0x01 value 0xBB` 作为状态机启停帧，仅接受 `value=0x01/0x00`。
- 非 `0xFF` / `0xAA` 字节直接忽略；控制帧 `FUNC`、`VALUE` 或 `EOF` 非法时丢弃当前帧，不改变状态机。
- 启停控制帧不携带 checksum，原因是只有 4 字节、只控制布尔开关，最小实现优先；需要抗干扰增强时再增加校验。

#### 3、RA6M5 -> Jetson 回传（旧协议，调试兼容）

旧协议状态回传格式保持 4 字节：

```text
[0xCC][FUNC][VALUE][0xDD]
```

当前回传定义如下：


|   FUNC | 宏                          |  VALUE | 当前含义                              |
| -------: | ----------------------------- | -------: | --------------------------------------- |
| `0x01` | `RA6_TO_JETSON_READY`       | `0x01` | 机械臂已到预定位，Jetson 可发送视觉帧 |
| `0x02` | `RA6_TO_JETSON_ALIGN_DONE`  | `0x01` | 连续稳定对准完成，等待输出许可        |
| `0x03` | `RA6_TO_JETSON_OUTPUT`      | `0x01` | 激光输出已进入激活状态                |
| `0x03` | `RA6_TO_JETSON_OUTPUT`      | `0x00` | 激光输出已切断                        |
| `0x04` | `RA6_TO_JETSON_TARGET_CTRL` | `0x01` | 定靶状态机已启用                      |
| `0x04` | `RA6_TO_JETSON_TARGET_CTRL` | `0x00` | 定靶状态机已关闭或启用失败            |
| `0xFE` | `RA6_TO_JETSON_ERROR`       | `0x01` | 当前实现用于限位/急停类错误上报       |

`jetson_send_status_u8()` 使用 `R_SCI_UART_Write()` 异步发送，并通过 `jetson_notify_tx_complete_from_isr()` 释放发送完成信号量，等待超时为 20 ms。

启停状态机回传示例：

```text
CC 04 01 DD   // RA6M5 回传：状态机已启用
CC 04 00 DD   // RA6M5 回传：状态机已关闭，或启动请求被拒绝后保持关闭
```

### 4、Jetson 视觉统一通信协议（正式项目演示协议）

正式项目演示统一采用 `A5 5A + CRC16` 二进制协议。原因很直接：视觉链路同时承担状态机启停、视觉误差、三视图拍摄点位、状态回传、安全距离和心跳，继续维护 `FF/AA/CC` 三套帧头帧尾会让解析和异常处理变复杂。统一二进制帧可以用长度、序号和 CRC 明确边界，串口丢字节或插入噪声时更容易恢复。

第 1~3 节旧 `FF/AA/CC` 协议只作为调试兼容链路保留，不作为完整项目演示的主协议。

#### 4.1 统一帧格式

```text
SOF0 SOF1 VER TYPE SEQ LEN PAYLOAD... CRC16_LO CRC16_HI
A5   5A   01  xx   xx  nn  ...        xx       xx
```


| 字段        | 含义                                          |
| ------------- | ----------------------------------------------- |
| `SOF0/SOF1` | 固定帧头`A5 5A`，用于重新同步                 |
| `VER`       | 协议版本，第一版固定`0x01`                    |
| `TYPE`      | 消息类型                                      |
| `SEQ`       | Jetson 每发一帧递增；RA6M5 回传时带回对应序号 |
| `LEN`       | `PAYLOAD` 字节数，最大建议 32 字节            |
| `PAYLOAD`   | 小端格式，按`TYPE` 解析                       |
| `CRC16`     | CRC16/MODBUS，计算范围为`VER` 到 `PAYLOAD`    |

CRC 错误、长度错误、未知 `TYPE` 的帧直接丢弃；若已经解析到完整帧但业务参数非法，RA6M5 回传 `ERROR`。

`SEQ` 是 `uint8`，从 `0x00~0xFF` 循环即可。Jetson 端不需要等待上一帧 ACK 才能递增，但状态控制类命令建议按状态回传推进，避免重复下发造成状态混乱。

CRC16/MODBUS 计算规则：

- 初值：`0xFFFF`
- 多项式：`0xA001`
- 计算范围：从 `VER` 字节开始，到最后一个 `PAYLOAD` 字节结束，不包含 `A5 5A` 和 CRC 本身。
- 发送顺序：低字节在前，即 `CRC16_LO CRC16_HI`。

Jetson 端 Python 参考实现：

```python
def crc16_modbus(data: bytes) -> int:
    crc = 0xFFFF
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 0x0001:
                crc = (crc >> 1) ^ 0xA001
            else:
                crc >>= 1
    return crc & 0xFFFF

def build_frame(msg_type: int, seq: int, payload: bytes) -> bytes:
    body = bytes([0x01, msg_type & 0xFF, seq & 0xFF, len(payload)]) + payload
    crc = crc16_modbus(body)
    return bytes([0xA5, 0x5A]) + body + crc.to_bytes(2, "little")
```

#### 4.2 消息类型


|   TYPE | 方向            | 名称           | PAYLOAD                                            |
| -------: | ----------------- | ---------------- | ---------------------------------------------------- |
| `0x01` | Jetson -> RA6M5 | `HEARTBEAT`    | `uint32 jetson_tick_ms`                            |
| `0x02` | Jetson -> RA6M5 | `TARGET_CTRL`  | `uint8 enable`，`1` 启动定靶，`0` 关闭定靶         |
| `0x03` | Jetson -> RA6M5 | `VISION_ERROR` | `int16 dcx` + `int16 dcy` + `uint8 valid`          |
| `0x04` | Jetson -> RA6M5 | `CAPTURE_CTRL` | `uint8 action` + `uint8 point_id`                  |
| `0x05` | Jetson -> RA6M5 | `SAFE_DISTANCE` | `uint16 distance_mm` + `uint8 valid`              |
| `0x81` | RA6M5 -> Jetson | `STATUS`       | `uint8 event` + `uint8 value` + `uint8 error_code` |
| `0xFE` | RA6M5 -> Jetson | `ERROR`        | `uint8 error_code`                                 |

Jetson -> RA6M5 payload 编码：

| TYPE | 名称 | payload 字节顺序 | 说明 |
|---:|---|---|---|
| `0x01` | `HEARTBEAT` | `tick0 tick1 tick2 tick3` | `uint32 jetson_tick_ms` 小端，用于链路在线判断 |
| `0x02` | `TARGET_CTRL` | `enable` | `0x01` 启动定靶，`0x00` 关闭定靶 |
| `0x03` | `VISION_ERROR` | `dcx_lo dcx_hi dcy_lo dcy_hi valid` | `dcx/dcy` 为 `int16` 小端，`valid=1` 表示本帧有效 |
| `0x04` | `CAPTURE_CTRL` | `action point_id` | 三视图拍摄和选择视图控制 |
| `0x05` | `SAFE_DISTANCE` | `distance_lo distance_hi valid` | `distance_mm` 为 `uint16` 小端，单位 mm |

RA6M5 -> Jetson payload 编码：

| TYPE | 名称 | payload 字节顺序 | 说明 |
|---:|---|---|---|
| `0x81` | `STATUS` | `event value error_code` | 正常状态回传，`error_code=0x00` 表示无错误 |
| `0xFE` | `ERROR` | `error_code` | 异常回传 |

`CAPTURE_CTRL.action` 定义：


| action | point_id | 含义                                     |
| -------: | ---------: | ------------------------------------------ |
| `0x01` |  `1/2/3` | 机械臂移动到第 1/2/3 个拍摄点位          |
| `0x02` |      `0` | 三视图拍摄完成，RA6M5 回 HOME            |
| `0x03` |  `1/2/3` | Jetson 选择黑痣所在视图，RA6M5 进入对应定靶预开始点 |

拍摄点位只允许 `point_id=1/2/3`。Jetson 不下发坐标、关节角或 `auto` 参数，只下发动作类型和点位编号。三个点位由 RA6M5 端固化为可验证的安全点位，避免视觉端误发坐标导致机械臂进入危险姿态。

RA6M5 端固化点位定义：

| point_id | 视图 | RA6M5 行为 |
| --------: | ---- | ---------- |
| `1` | 左视图 | 执行左侧拍摄安全点位，到位后回传 `STATUS event=0x10 value=1` |
| `2` | 正视图 | 执行正面拍摄安全点位，到位后回传 `STATUS event=0x10 value=2` |
| `3` | 右视图 | 执行右侧拍摄安全点位，到位后回传 `STATUS event=0x10 value=3` |

当前调试配方仅作为后续固化到 RA6M5 端的来源，不进入通信帧：

```text
point_id=1 左视图来源:
soft_reset
auto 0 -130 -15
abs_rotate 0 65
abs_rotate 3 330
abs_rotate 4 85

point_id=2 正视图来源:
soft_reset
auto 0 -130 -15
abs_rotate 0 90

point_id=3 右视图来源:
soft_reset
auto 0 -130 -15
abs_rotate 0 115
rel_rotate 3 35
abs_rotate 4 80
```

`SAFE_DISTANCE` 由 Jetson 双目相机下发，单位固定为 mm。默认安全距离为 `100 mm`，释放阈值为 `110 mm`，避免距离在边界附近抖动时反复进入/退出保护。示例：

```text
A5 5A 01 05 SEQ 03 distance_lo distance_hi valid CRC16_LO CRC16_HI
```

典型帧示例，可作为 Jetson 端单元测试用例：

| 场景 | HEX |
|---|---|
| `HEARTBEAT seq=1 tick_ms=1234` | `A5 5A 01 01 01 04 D2 04 00 00 19 AF` |
| `TARGET_CTRL start seq=2` | `A5 5A 01 02 02 01 01 79 E8` |
| `TARGET_CTRL stop seq=3` | `A5 5A 01 02 03 01 00 E9 E8` |
| `VISION_ERROR seq=4 dcx=-7 dcy=-50 valid=1` | `A5 5A 01 03 04 05 F9 FF CE FF 01 4F 2F` |
| `SAFE_DISTANCE seq=5 distance=120mm valid=1` | `A5 5A 01 05 05 03 78 00 01 86 08` |
| `CAPTURE_CTRL seq=6 action=0x01 point_id=1` | `A5 5A 01 04 06 02 01 01 91 12` |
| `CAPTURE_CTRL seq=7 action=0x02 point_id=0` | `A5 5A 01 04 07 02 02 00 51 DE` |
| `CAPTURE_CTRL seq=8 action=0x03 point_id=3` | `A5 5A 01 04 08 02 03 03 13 5B` |

#### 4.3 RA6M5 状态事件

`STATUS.event` 沿用现有状态语义，并扩展拍摄点位事件：


|  event |       value | 含义                                  |
| -------: | ------------: | --------------------------------------- |
| `0x01` |      `0x01` | 定靶预定位完成，Jetson 可发送视觉误差 |
| `0x02` |      `0x01` | 视觉对准完成，等待 P000 许可          |
| `0x03` | `0x01/0x00` | P801 输出开启/关闭                    |
| `0x04` | `0x01/0x00` | 定靶状态机启用/关闭结果               |
| `0x05` |      `0x01` | 心跳有效，视觉链路在线                |
| `0x06` | `0x01/0x00` | 安全距离正常/过近                    |
| `0x10` |     `1/2/3` | 已到达第 1/2/3 个拍摄点位             |
| `0x11` |      `0x01` | 三视图拍摄结束并已回 HOME             |
| `0x12` |     `1/2/3` | 已进入 Jetson 选择的定靶预开始点       |

RA6M5 新协议回传示例：

| 场景 | HEX |
|---|---|
| `STATUS READY seq=9` | `A5 5A 01 81 09 03 01 01 00 99 C4` |
| `ERROR HEARTBEAT_TIMEOUT seq=10` | `A5 5A 01 FE 0A 01 07 48 78` |

Jetson 解析 `STATUS` 时只需要先看 `event/value`，`error_code=0x00` 表示无错误；解析 `ERROR` 时只需要读取 `error_code`。

错误码建议：


| error_code | 含义                         |
| -----------: | ------------------------------ |
|     `0x00` | 无错误                       |
|     `0x02` | 未知消息类型                 |
|     `0x03` | payload 长度或参数非法       |
|     `0x04` | pose invalid，未 soft_reset  |
|     `0x05` | 机械臂忙，当前不能接收新点位 |
|     `0x06` | 点位不可达或 IK 失败         |
|     `0x07` | Jetson 心跳超时              |
|     `0x08` | 限位、急停或安全输入触发     |
|     `0x09` | 双目安全距离过近             |

#### 4.4 心跳与安全策略

Jetson 打开串口后应每 `100~200 ms` 发送一次 `HEARTBEAT`。RA6M5 超过 `600 ms` 没收到合法心跳时，判定视觉链路离线，立即执行安全动作：

1. 关闭 P801。
2. 中断三视图拍摄流程或定靶状态机中的当前危险动作。
3. 清除视觉数据有效标志。
4. 回传 `ERROR 0x07`。

心跳只证明 Jetson 在线，不能代替 P000 发射许可，也不作为普通手动 `auto` 的全局急停。P801 输出仍必须满足：状态机启用、视觉稳定、P000 按住、无急停/限位/超时。

#### 4.5 安全距离策略

`SAFE_DISTANCE` 是正式演示链路中的安全输入，目标是覆盖三视图拍摄点位和定靶状态机，而不是只服务 `target_enable` 之后的输出阶段。普通手动 `auto`、单关节调试和上位机人工调试命令不纳入本轮自动安全回退范围。Jetson 应周期发送双目测得的 `distance_mm` 和 `valid`，RA6M5 根据距离决定是否允许继续靠近目标。

- `distance_mm < 100`：立即关闭 P801，中断正在靠近目标的动作，回传 `STATUS 0x06 value=0` 和 `ERROR 0x09`，并执行 Y 方向后退或进入安全恢复。
- `distance_mm >= 110`：释放安全距离保护，回传 `STATUS 0x06 value=1`，允许继续三视图拍摄点位移动、视觉对准和 P000 输出确认。
- 距离数据超过 `500 ms` 未更新或 `valid=0`：视为距离不可用，禁止进入 `TARGET_OUTPUT`，三视图拍摄点位和定靶靠近动作应进入等待或恢复。

安全距离不能替代 P000。即使距离安全，P801 输出仍必须满足视觉稳定、P000 按住、心跳有效和无限位触发。

#### 4.6 三视图拍摄流程

三视图拍摄由 Jetson 主导流程，RA6M5 执行固化安全点位并回传到位结果。Jetson 只发送 `CAPTURE_CTRL action + point_id`，不发送具体坐标、关节角或 `auto` 参数。

`action=0x01` 的语义是“移动到拍摄点并停留”，不得在到位后自动回 HOME；只有 `action=0x02 point_id=0` 表示三视图拍摄完成并回 HOME。

```text
Jetson 周期发送 HEARTBEAT
Jetson -> RA6M5: CAPTURE_CTRL action=0x01 point_id=1
RA6M5 -> Jetson: STATUS event=0x10 value=1
Jetson 拍摄左视图

Jetson -> RA6M5: CAPTURE_CTRL action=0x01 point_id=2
RA6M5 -> Jetson: STATUS event=0x10 value=2
Jetson 拍摄正视图

Jetson -> RA6M5: CAPTURE_CTRL action=0x01 point_id=3
RA6M5 -> Jetson: STATUS event=0x10 value=3
Jetson 拍摄右视图

Jetson -> RA6M5: CAPTURE_CTRL action=0x02 point_id=0
RA6M5 -> Jetson: STATUS event=0x11 value=1

Jetson 在 QT 界面选择黑痣所在视图
Jetson -> RA6M5: CAPTURE_CTRL action=0x03 point_id=1/2/3
RA6M5 -> Jetson: STATUS event=0x12 value=1/2/3

Jetson -> RA6M5: TARGET_CTRL enable=1
RA6M5 -> Jetson: STATUS event=0x04 value=1
RA6M5 -> Jetson: STATUS READY value=1
后续继续沿用视觉误差、对准、P000 许可、P801 输出流程
```

  拍摄流程中不允许打开 P801；P801 只属于定靶状态机的 `OUTPUT` 阶段。

#### 4.7 Jetson 端联调流程

定靶链路最小流程：

```text
1. Jetson 打开串口
2. 周期发送 HEARTBEAT，周期 100~200 ms
3. 周期发送 SAFE_DISTANCE，例如 distance=120 valid=1
4. 发送 TARGET_CTRL enable=1
5. 等待 STATUS event=0x04 value=1，表示状态机启用成功
6. 等待 STATUS event=0x01 value=1，表示 RA6M5 已到预定位 READY
7. 周期发送 VISION_ERROR dcx/dcy valid=1
8. 等待 STATUS event=0x02 value=1，表示视觉对准完成
9. UI 提示用户按住 P000
10. 等待 STATUS event=0x03 value=1，表示 P801 输出开启
11. 松开 P000 或关闭状态机后，等待 STATUS event=0x03 value=0
12. 发送 TARGET_CTRL enable=0 关闭状态机
```

三视图项目演示流程：

```text
1. Jetson 周期发送 HEARTBEAT 和 SAFE_DISTANCE
2. 发送 CAPTURE_CTRL action=0x01 point_id=1
3. 等待 STATUS event=0x10 value=1 后拍摄左视图
4. 发送 CAPTURE_CTRL action=0x01 point_id=2
5. 等待 STATUS event=0x10 value=2 后拍摄正视图
6. 发送 CAPTURE_CTRL action=0x01 point_id=3
7. 等待 STATUS event=0x10 value=3 后拍摄右视图
8. 发送 CAPTURE_CTRL action=0x02 point_id=0
9. 等待 STATUS event=0x11 value=1
10. Jetson 在三张图中完成黑痣选择，得到 point_id=1/2/3
11. 发送 CAPTURE_CTRL action=0x03 point_id=选中的视图编号
12. 等待 STATUS event=0x12 value=选中的视图编号
13. 发送 TARGET_CTRL enable=1，进入定靶链路
```

异常处理建议：

- 收到 `ERROR 0x07`：Jetson 心跳超时，立即停止发送视觉误差，UI 显示链路异常。
- 收到 `ERROR 0x09` 或 `STATUS event=0x06 value=0`：距离过近，暂停定靶/拍摄推进，等待距离恢复。
- 超过预期时间没有收到下一状态：不要重复快速发送控制命令，应先显示超时并等待人工确认。
