# 6轴机械臂控制项目报告

## 1. 报告范围

本报告只整理 `Robot_FreeRTOS` 工程中单独机械臂的控制流程、主要实现方式和当前存在的问题。视觉采摘、底盘控制、MQTT 联网、环境传感器等模块只在与机械臂控制入口有关时提及，不作为重点分析对象。

工程目标硬件为 Renesas RA6M5 主控板，芯片型号 `R7FA6M5BF2CBG`。系统基于 FreeRTOS，机械臂关节使用 42 闭环步进电机，通过 CAN 总线通信。

## 2. 代码分层与关键文件

### 2.1 分层

- `USER_BSP`：底层驱动层，主要包含 CAN、UART、LED、DHT11、步进电机协议封装。
- `Middle`：中间层，主要包含运动学、命令解析、夹爪、底盘、K230 通信等。
- `APP`：应用层，机械臂核心控制集中在 `APP/robot.c` 和 `APP/robot.h`。
- `src`：FreeRTOS 线程入口，`src/robot_thread_entry.c` 负责初始化硬件和启动应用层任务。

### 2.2 机械臂相关核心文件

- `src/robot_thread_entry.c`：系统主线程入口，初始化 UART、CAN、底盘应用后调用 `robot_init()`。
- `APP/robot.h`：机械臂宏定义、状态位、关节结构体、事件类型、全局对象和外部接口声明。
- `APP/robot.c`：机械臂控制核心，包括关节参数、复位、事件队列、路径插补、逆解调用、PID 速度控制、CAN 反馈读取。
- `Middle/robot_kinematics.c`：机械臂逆运动学实现，生成多组候选解、角度映射、最优解选择。
- `Middle/robot_cmd.c`：UART/MQTT 指令解析，将 `auto`、`abs_rotate`、`rel_rotate`、`soft_reset`、`hard_reset` 等指令转换为机械臂事件。
- `USER_BSP/Emm_V5.c`：闭环步进电机 CAN 协议封装，包括使能、速度模式、位置模式、停止、读取参数等。
- `USER_BSP/bsp_can.c`：RA6M5 CANFD 外设封装，当前作为标准 CAN 帧发送，使用扩展 ID，提供 CAN 总线互斥锁和请求-应答等待接口。

## 3. 机械臂总体控制流程

### 3.1 系统启动流程

1. `robot_thread_entry()` 启动后初始化 LED、UART 和 CAN。
2. 调用 `BSP_CAN_Init()` 打开 `g_canfd0`，创建 CAN 接收信号量和 CAN 总线互斥锁。
3. 调用 `robot_chassis_app_init()` 初始化底盘应用，但这不是机械臂主流程。
4. 调用 `robot_init()` 初始化机械臂控制模块。
5. `robot_init()` 打开 6 路外部限位中断，复制关节默认参数，初始化末端复位矩阵，创建：
   - `g_robot.event_queue`：机械臂控制事件队列。
   - `g_robot.cmd_queue`：指令解析队列。
   - `robot_control_task`：机械臂运动控制任务，优先级 3。
   - `robot_cmd_service`：指令解析任务，优先级 2。

### 3.2 指令到运动的流程

机械臂采用“命令队列 + 事件队列”的两级调度方式：

1. 串口或 MQTT 收到文本指令后，进入 `g_robot.cmd_queue`。
2. `robot_cmd_service()` 从命令队列取出指令。
3. `robot_uart1_handle()` 或 `robot_mqtt_handle()` 解析指令。
4. 指令处理函数调用 `robot_send_xxx_event()`，把机械臂动作转换成 `struct robot_event`。
5. `robot_control_task()` 从 `g_robot.event_queue` 取事件并执行实际控制。

常用指令和事件关系：

- `rel_rotate <joint> <angle>` -> `ROBOT_JOINT_REL_ROTATE`
- `abs_rotate <joint> <angle>` -> `ROBOT_JOINT_ABS_ROTATE`
- `auto <x> <y> <z>` -> `ROBOT_AUTO_EVENT`
- `soft_reset` -> `ROBOT_SOFT_RESET_EVENT`
- `hard_reset` -> `ROBOT_HARD_RESET_EVENT`
- `remote_enable` / `remote_event` -> `ROBOT_REMOTE_CONTROL_EVENT`

### 3.3 单关节旋转流程

单关节相对/绝对旋转由 `robot_joint_rotate_to()` 实现：

1. 检查关节 ID 和速度参数。
2. 根据目标角度和当前角度计算相对旋转角度。
3. 根据关节正方向 `postive_direction` 得到电机物理方向。
4. 按减速比和 3200 细分步数计算目标脉冲数。
5. 调用 `Emm_V5_Pos_Control()` 发送位置模式 CAN 指令。

当前单关节位置模式主要是开环下发目标步数，代码中会提前更新 `joint->current_angle`，不等待实际到位反馈闭环确认。

### 3.4 自动笛卡尔运动流程

`auto <x> <y> <z>` 是当前整臂运动的主流程：

1. `robot_send_auto_event()` 检查当前末端位姿是否有效。
2. `robot_auto_move_interpolation()` 根据当前末端位置 `g_robot.cur_pos` 和目标位置生成直线路径。
3. `robot_path_interpolation_linear()` 按 `ROBOT_INTERPOLATION_RESOLUTION = 1.0 mm` 生成路径点。
4. 对每个路径点：
   - 用 `robot_kinematics_cal_T()` 在复位姿态矩阵 `T_0_6_reset` 基础上叠加目标平移。
   - 调用 `robot_kinematics_inverse()` 求解关节角度。
   - 将 J6 锁定为当前角度，避免夹爪随整臂轨迹抖动。
   - 将本次逆解结果更新到运动学模块，用于下一路径点选解。
5. 全部路径点逆解完成后，调用 `robot_pid_run()` 执行轨迹。
6. 运动完成后更新 `g_robot.cur_pos`，并尝试刷新 J1-J5 的实际角度反馈。

### 3.5 PID 轨迹执行流程

`robot_pid_run()` 的核心思想是：路径点已经离线逆解成目标关节角数组，运行时每个插补周期把目标角度交给 PID，PID 输出每个关节的速度命令。

执行逻辑：

1. 每个路径点间隔 `ROBOT_INTERPOLATION_TIME_RESOLUTION = 100 ms`。
2. 对 J1-J5 准备当前路径点的目标角度，J6 不参与整臂 PID。
3. 在到达当前路径点结束时间前，循环调用 `robot_pid_one_period()`。
4. `robot_pid_one_period()` 每 20 ms 计划执行一次：
   - 逐个读取关节当前角度。
   - 计算当前角度到目标角度的最短角度误差。
   - 用 `KP=10.0`、`KI=0.002`、`KD=0.0` 计算速度。
   - 调用 `robot_joint_veloccity_to()` 下发速度模式 CAN 指令。
5. 轨迹结束后停止 J1-J5。

### 3.6 复位流程

硬复位 `robot_joint_hard_reset()`：

1. 按安全顺序 `J5 -> J4 -> J2 -> J3 -> J1` 逐关节复位。
2. 使能当前电机。
3. 使能限位触发逻辑。
4. 沿复位方向运动，直到限位开关触发。
5. 调用 `Emm_V5_Reset_CurPos_To_Zero()` 清零电机当前位置。
6. 全部完成后将 J1-J6 的软件角度设置为默认初始值。

软复位 `robot_joint_soft_reset()`：

1. 只处理 J1-J5，J6 作为夹爪相关轴跳过。
2. 读取当前角度，映射到软限位范围。
3. 按安全顺序控制各关节回到默认初始角度。
4. 通过反馈验证 home 姿态。
5. 如果误差超限，进入 refine 修正循环。
6. 验证通过则设置 `ROBOT_STATUS_POSE_VALID`，否则进入 `ROBOT_STATUS_POSE_DEGRADED`。

## 4. 关键实现细节

### 4.1 关节参数

`APP/robot.c` 中 `g_joints_init` 定义了 6 个关节的默认角度、方向、减速比、限位中断、软限位和复位方向：

- J1：默认 90 度，范围 0-360，减速比 50。
- J2：默认 90 度，范围 90-180，减速比 99.99。
- J3：默认 -90 度，范围 -90-90，减速比 50.89。
- J4：默认 0 度，范围 -90-90，减速比 51。
- J5：默认 90 度，范围 0-90，减速比 26.85。
- J6：默认 0 度，范围 0-360，减速比 51。

当前整臂控制宏 `ROBOT_ARM_JOINT_NUM` 定义为 `ROBOT_MAX_JOINT_NUM - 1`，即整臂轨迹只控制 J1-J5。J6 在 `auto` 和 `time_func` 中锁定当前角度，更多作为夹爪模块独立控制。

### 4.2 运动学实现

`Middle/robot_kinematics.c` 使用 DH 参数和解析公式求解逆运动学：

1. 根据目标矩阵依次计算 `theta3`、`theta2`、`theta1`、`theta5`、`theta4`、`theta6`。
2. 最多保留 4 组候选解。
3. 将弧度转换为 0-360 度。
4. 使用各关节软限位做角度映射，超限则标记该组解无效。
5. 使用 `joint_weight = {5, 3, 3, 1, 1, 0}` 选择与当前关节角度加权差值最小的候选解。

该实现适合在固定结构机械臂上快速求逆解，但目前没有雅可比矩阵、奇异值、阻尼最小二乘或路径级连续性约束。

### 4.3 CAN 与电机控制

电机协议在 `USER_BSP/Emm_V5.c` 中封装：

- `Emm_V5_En_Control()`：电机使能。
- `Emm_V5_Vel_Control()`：速度模式。
- `Emm_V5_Pos_Control()`：位置模式。
- `Emm_V5_Stop_Now()`：立即停止。
- `Emm_V5_Read_Sys_Params()`：读取电机状态或位置。
- `Emm_V5_Reset_CurPos_To_Zero()`：清零当前位置。

CAN 底层在 `USER_BSP/bsp_can.c` 中实现：

- 使用 RA FSP 的 `R_CANFD_Write()`。
- 使用扩展帧 ID，电机地址放在扩展 ID 高 8 位。
- 发送标准 CAN 2.0 数据帧，单帧最多 8 字节。
- 超过单帧的数据会分包发送。
- `BSP_CAN_Lock()` / `BSP_CAN_Unlock()` 用于串行化请求-应答事务。
- `BSP_CAN_WaitReply()` 按电机地址和功能码等待匹配回包。

## 5. 当前存在的问题

### 5.1 机械臂运行不够丝滑

主要原因不是单一参数问题，而是当前控制链路存在多个离散和阻塞点：

1. 轨迹点是 1 mm 空间离散 + 100 ms 时间离散，目标关节角以阶梯形式变化。
2. PID 周期配置为 20 ms，但每个 PID 周期内需要对多个关节逐个执行 CAN 读角度和 CAN 速度下发。
3. J1-J5 每周期最多包含 5 次位置读取和 5 次速度命令，CAN 请求-应答串行执行，实际周期很容易超过 20 ms。
4. PID 输出直接变成速度命令，没有速度前馈、加速度连续约束、S 曲线或梯形速度规划。
5. 不同路径点之间没有关节速度连续性检查，逆解角度变化可能在某些位置突然增大。
6. `robot_pid_run()` 以固定路径点时间为目标，但如果 `robot_pid_one_period()` 阻塞超时，轨迹实际时间会漂移或跳过细腻控制。

结果表现为：关节速度命令容易阶跃，CAN 通信负载高时控制周期抖动，末端运动不够连续。

### 5.2 奇异点穿越问题

当前代码对奇异点只有局部公式兜底，没有完整的奇异点穿越策略：

1. 逆解中 `theta5_zyz` 接近 0 或 pi 时，`theta4` 直接返回 0，`theta6` 走特殊分支。
2. 这种处理可以避免 NaN，但不能保证腕部姿态和关节角连续。
3. 最优解选择只比较候选解与当前关节角的加权绝对差，没有显式锁定上一组解的分支。
4. 对 0-360 度关节，最优解选择没有统一使用圆周最短角度差，跨 0 度附近可能把 1 度到 359 度误判为 358 度大跳变。
5. 没有计算雅可比条件数或奇异阈值，无法提前减速、绕行或切换控制策略。
6. 笛卡尔直线插补不考虑关节空间可达性和奇异区域，可能把路径直接穿过腕部或肘部奇异构型。

因此，当路径经过奇异区或逆解分支边界时，关节角可能突然跳变，导致机械臂抖动、绕大圈或动作不连续。

### 5.3 CAN 通信周期和实时性风险

当前 CAN 接收侧使用全局单缓冲区 + binary semaphore。虽然请求-应答接口增加了互斥锁，但仍有以下风险：

1. 如果多个回包在任务处理前连续到达，后到帧会覆盖全局接收缓冲。
2. binary semaphore 不记录多帧数量，存在丢中间帧的结构性风险。
3. 速度 PID 控制中频繁读位置和下发速度，会显著增加总线负载。
4. 部分位置控制、使能、清零等函数直接调用 `Emm_V5_xxx()`，没有统一进入请求-应答锁和反馈确认流程。
5. 底盘、夹爪、机械臂共用 CAN 时，如果上层没有统一仲裁，机械臂控制周期会被其他模块影响。

这类问题会直接影响运动平滑性和到位可靠性。

### 5.4 位置模式缺少到位闭环确认

`robot_joint_rotate_to()` 下发位置模式后会立即更新 `joint->current_angle`，但没有等待电机到位，也没有读取实际位置确认。

影响：

- 软件估计角度可能领先于真实机械位置。
- 后续逆解会基于估计角度选解，误差会累积。
- 单关节 `abs_rotate` / `rel_rotate` 结束时不能判断是否真正到位。
- 如果电机丢步、堵转或 CAN 指令失败，上层状态不一定及时反映。

### 5.5 当前末端位姿模型较简化

`robot_kinematics_cal_T()` 当前只在固定复位姿态矩阵上叠加 x/y/z 平移，不处理末端姿态变化。

影响：

- `auto` 只支持固定姿态下的笛卡尔位置运动。
- 末端姿态调整能力没有真正纳入路径规划。
- 奇异点附近无法通过姿态调整绕开高风险构型。
- J6 被锁定后，整臂轨迹实际上是 J1-J5 的固定姿态位置控制，不是完整 6 轴联动。

### 5.6 初始化和错误处理存在可疑点

`robot_init()` 中外部中断 `open()` 的返回判断写成了 `== FSP_SUCCESS` 时打印 `open failed`。如果代码按字面执行，成功打开时会打印失败日志，容易误导调试。

此外，多个关键函数只打印错误并继续，缺少统一错误状态、故障码和恢复策略。例如：

- CAN 读角度失败后进入 degraded 模式，但后续仍可能用估计位姿继续运动。
- IK 某路径点失败时直接中止本次运动，没有路径回退或安全停靠点。
- PID 输出速度没有限幅和积分抗饱和处理。

## 6. 后续改进建议

### 6.1 运动平滑性

- 将轨迹规划从“位置点 + PID 追踪”升级为关节空间梯形速度或 S 曲线规划。
- 对每个路径点计算关节角、关节速度和关节加速度连续性。
- 减少 PID 周期内的 CAN 同步读位置次数，改为周期性批量反馈或驱动器主动上报。
- 对 PID 输出速度增加限幅、加速度限幅和积分限幅。
- 将 `ROBOT_PID_PERIOD` 和 CAN 实际耗时做实测，确认控制周期能稳定达成。

### 6.2 奇异点处理

- 在逆解后增加路径级连续性检查，拒绝或重规划关节角突变过大的路径点。
- 对 0-360 度关节统一使用圆周最短角度差选解。
- 增加逆解分支锁定策略，优先沿用上一时刻的同一分支。
- 增加奇异点检测，例如腕部 `theta5` 接近 0/pi 时提前标记风险。
- 在奇异区附近降低末端速度，必要时改用关节空间插补绕行。
- 长期可考虑增加雅可比矩阵条件数检测和阻尼最小二乘速度逆解。

### 6.3 状态反馈和安全

- 位置模式命令下发后等待驱动器到位回包，或读取实际角度确认。
- CAN 接收改为环形队列，避免单缓冲覆盖。
- 建立统一 CAN 仲裁层，让机械臂、夹爪、底盘共享总线时有明确优先级。
- 将 `POSE_VALID`、`POSE_DEGRADED`、CAN 超时、IK 失败、限位触发等状态统一成故障码。
- 对自动运动增加“规划成功后再执行”的安全检查，执行中失败应立即停机并保持故障状态。

## 7. 小结

当前工程已经具备完整的机械臂控制骨架：FreeRTOS 任务调度、命令解析、事件队列、关节参数管理、限位复位、解析逆解、笛卡尔直线插补、PID 速度控制和 CAN 电机通信都已经串起来。

当前主要短板集中在运动质量和稳定性：控制周期被 CAN 串行请求-应答拉长，轨迹规划缺少速度/加速度连续性，逆解选解缺少跨奇异点和跨 0 度的连续约束。后续如果优先解决“平滑轨迹规划 + 逆解连续性 + CAN 反馈实时性”三件事，机械臂动作丝滑度和奇异点穿越稳定性会有明显改善。
